{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "context-menu",
  "type": "registry:ui",
  "title": "Context Menu",
  "description": "Displays a menu to the user — such as a set of actions or functions — triggered by right click.",
  "dependencies": [
    "@zag-js/menu",
    "marko-zag"
  ],
  "registryDependencies": [
    "http://localhost:3000/r/utils.json",
    "http://localhost:3000/r/styles/mira/icon.json"
  ],
  "files": [
    {
      "path": "ui/context-menu/context-menu.marko",
      "type": "registry:file",
      "target": "~/src/components/ui/context-menu/context-menu.marko",
      "content": "import * as menu from \"@zag-js/menu\";\nimport { positionerStyle } from \"marko-zag\";\nimport { cn } from \"../../../lib/utils.ts\";\nimport Icon from \"../icon/icon.marko\";\nimport Submenu from \"./submenu.marko\";\n\nexport interface ContextMenuItem {\n  type?: \"item\" | \"separator\" | \"label\" | \"checkbox\" | \"radio\" | \"sub\";\n  value?: string;\n  label?: string;\n  disabled?: boolean;\n  shortcut?: string;\n  /** Upstream `variant: \"default\" | \"destructive\"`, emitted as `data-variant`. */\n  variant?: \"default\" | \"destructive\";\n  /** Emits `data-inset` — indents the entry to align with icon-bearing siblings. */\n  inset?: boolean;\n  /** Checked state for `type=\"checkbox\"` and `type=\"radio\"` entries. */\n  checked?: boolean;\n  /** Groups radio items — required for `type: \"radio\"` entries. */\n  radioGroup?: string;\n  /** `type: \"radio\"` entries only — this entry's value within its `radioGroup`. */\n  radioValue?: string;\n  /**\n   * `type: \"sub\"` entries only — the nested submenu's own item list. One\n   * level deep: a `type: \"sub\"` entry inside a submenu's own `subEntries` is\n   * not wired (same limit as menubar's submenu).\n   */\n  subEntries?: ContextMenuItem[];\n}\n\n// Attr-tag form of ContextMenuItem. A SINGLE attr tag name (`item`) carries\n// all five kinds (discriminated by `type`) rather than separate per-kind\n// attr tags: Marko collects each attr-tag NAME into its own independent\n// iterable on `input` with no shared ordering between differently-named\n// siblings, so distinct tag names could not preserve call-site interleaving.\n// One name with a `type` discriminant keeps document order intact by\n// construction (same design as dropdown-menu.marko). `content` lets an item\n// or label carry a markup body; the plain `label` string renders as the\n// fallback when no body is supplied.\nexport interface ContextMenuItemAttrs extends ContextMenuItem {\n  content?: Marko.Body;\n}\n\nexport interface Input {\n  content: Marko.Body;\n  items?: ContextMenuItem[];\n  item?: Marko.AttrTag<ContextMenuItemAttrs>;\n  select?: (value: string) => void;\n  class?: string;\n}\n\n<machine-props/machineProps from=input pick=menu.props\n  onSelect(details: menu.SelectionDetails) {\n    input.select?.(details.value);\n  }/>\n<service/service machine=() => menu.machine props=machineProps/>\n<connect/api=(service, normalizeProps) =>\n  menu.connect(service, normalizeProps)\n  service=service\n/>\n\n<const/{ class: className }=input/>\n\n// Normalize whichever source was supplied into ONE entries list so the\n// render path below never branches. Attr tags win when present (compound is\n// primary per the compound-tag-dx report); `items=` is the fallback. The\n// single `<@item>` tag name preserves true document order by construction,\n// so interleaved item/separator/label entries render exactly as written.\n// `items=` entries never carry a body, so each is mapped into the\n// ContextMenuItemAttrs shape with an explicit undefined `content` — no\n// casts needed (mirrors accordion/toggle-group).\n<const/attrItems=[...(input.item ?? [])]/>\n<const/entries=(attrItems.length > 0 ? attrItems : (input.items ?? []).map((item) => ({ ...item, content: undefined as Marko.Body | undefined })))/>\n\n// Getter handed to each <Submenu> child so it can call `setParent` on this\n// menu's raw service (see submenu.marko for the full wiring rationale).\n<const/getOwnService=() => service.service/>\n\n<div ...api().getContextTriggerProps() data-slot=\"context-menu-trigger\" class=\"select-none\">\n  <${input.content}/>\n</div>\n<portal>\n  <if=api().open>\n    <div ...api().getPositionerProps() style=positionerStyle data-slot=\"context-menu-positioner\">\n      <div\n        ...api().getContentProps()\n        data-slot=\"context-menu-content\"\n        class=cn(\n          \"data-open:animate-in data-closed:animate-out data-closed:fade-out-0 data-open:fade-in-0 data-closed:zoom-out-95 data-open:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 ring-foreground/10 bg-popover text-popover-foreground min-w-32 rounded-lg p-1 shadow-md ring-1 duration-100 mu-menu-target mu-menu-translucent z-50 max-h-(--radix-context-menu-content-available-height) origin-(--radix-context-menu-content-transform-origin) overflow-x-hidden overflow-y-auto\",\n          className,\n        )\n      >\n        <for|entry, index| of=entries>\n          <if=entry.type === \"separator\">\n            <div ...api().getSeparatorProps() data-slot=\"context-menu-separator\" class=\"bg-border/50 -mx-1 my-1 h-px\"/>\n          </if>\n          <else if=entry.type === \"label\">\n            <div data-slot=\"context-menu-label\" data-inset=entry.inset || undefined class=\"text-muted-foreground px-2 py-1.5 text-xs data-inset:pl-7.5\">\n              <if=entry.content>\n                <${entry.content}/>\n              </if>\n              <else>\n                ${entry.label}\n              </else>\n            </div>\n          </else>\n          <else if=entry.type === \"checkbox\">\n            <div\n              ...api().getOptionItemProps({ type: \"checkbox\", value: entry.value ?? `item-${index}`, checked: entry.checked ?? false, disabled: entry.disabled })\n              data-slot=\"context-menu-checkbox-item\"\n              data-inset=entry.inset || undefined\n              class=\"focus:bg-accent focus:text-accent-foreground focus:**:text-accent-foreground min-h-7 gap-2 rounded-md py-1.5 pr-8 pl-2 text-xs data-inset:pl-7.5 [&_svg:not([class*='size-'])]:size-3.5 relative flex cursor-default items-center outline-hidden select-none data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0\"\n            >\n              <span ...api().getItemIndicatorProps({ value: entry.value ?? `item-${index}`, checked: entry.checked ?? false }) data-slot=\"context-menu-checkbox-item-indicator\" class=\"absolute right-2 flex items-center justify-center pointer-events-none\">\n                <if=entry.checked>\n                  <Icon name=\"Check\"/>\n                </if>\n              </span>\n              <if=entry.content>\n                <${entry.content}/>\n              </if>\n              <else>\n                ${entry.label}\n              </else>\n              <if=entry.shortcut>\n                <span data-slot=\"context-menu-shortcut\" class=\"text-muted-foreground group-focus/context-menu-item:text-accent-foreground ml-auto text-[0.625rem] tracking-widest\">${entry.shortcut}</span>\n              </if>\n            </div>\n          </else>\n          <else if=entry.type === \"radio\">\n            <div\n              ...api().getOptionItemProps({ type: \"radio\", value: entry.value ?? `item-${index}`, checked: entry.checked ?? false, disabled: entry.disabled })\n              data-slot=\"context-menu-radio-item\"\n              data-inset=entry.inset || undefined\n              class=\"focus:bg-accent focus:text-accent-foreground focus:**:text-accent-foreground min-h-7 gap-2 rounded-md py-1.5 pr-8 pl-2 text-xs data-inset:pl-7.5 [&_svg:not([class*='size-'])]:size-3.5 relative flex cursor-default items-center outline-hidden select-none data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0\"\n            >\n              <span ...api().getItemIndicatorProps({ value: entry.value ?? `item-${index}`, checked: entry.checked ?? false }) data-slot=\"context-menu-radio-item-indicator\" class=\"pointer-events-none\">\n                <if=entry.checked>\n                  <Icon name=\"Check\"/>\n                </if>\n              </span>\n              <if=entry.content>\n                <${entry.content}/>\n              </if>\n              <else>\n                ${entry.label}\n              </else>\n              <if=entry.shortcut>\n                <span data-slot=\"context-menu-shortcut\" class=\"\">${entry.shortcut}</span>\n              </if>\n            </div>\n          </else>\n          <else if=entry.type === \"sub\">\n            <Submenu\n              entries=entry.subEntries ?? []\n              label=entry.label\n              disabled=entry.disabled\n              inset=entry.inset\n              parent=getOwnService\n              onChildReady=(childService) => api().setChild(childService)\n            >\n              <@trigger|childApi|>\n                <div\n                  ...api().getTriggerItemProps(childApi)\n                  data-slot=\"context-menu-sub-trigger\"\n                  data-inset=entry.inset || undefined\n                  class=\"focus:bg-accent focus:text-accent-foreground data-open:bg-accent data-open:text-accent-foreground not-data-[variant=destructive]:focus:**:text-accent-foreground min-h-7 gap-2 rounded-md px-2 py-1 text-xs data-inset:pl-7.5 [&_svg:not([class*='size-'])]:size-3.5 flex cursor-default items-center outline-hidden select-none [&_svg]:pointer-events-none [&_svg]:shrink-0\"\n                >\n                  ${entry.label}\n                  <Icon name=\"ChevronRight\" class=\"mu-rtl-flip ml-auto\"/>\n                </div>\n              </@trigger>\n            </Submenu>\n          </else>\n          <else>\n            <div\n              ...api().getItemProps({ value: entry.value ?? `item-${index}`, disabled: entry.disabled })\n              data-slot=\"context-menu-item\"\n              data-inset=entry.inset || undefined\n              data-variant=entry.variant ?? \"default\"\n              class=\"focus:bg-accent focus:text-accent-foreground data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 dark:data-[variant=destructive]:focus:bg-destructive/20 data-[variant=destructive]:focus:text-destructive data-[variant=destructive]:*:[svg]:text-destructive not-data-[variant=destructive]:focus:**:text-accent-foreground min-h-7 gap-2 rounded-md px-2 py-1 text-xs/relaxed data-inset:pl-7.5 [&_svg:not([class*='size-'])]:size-3.5 group/context-menu-item relative flex cursor-default items-center outline-hidden select-none data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0\"\n            >\n              <if=entry.content>\n                <${entry.content}/>\n              </if>\n              <else>\n                ${entry.label}\n              </else>\n              <if=entry.shortcut>\n                <span data-slot=\"context-menu-shortcut\" class=\"\">${entry.shortcut}</span>\n              </if>\n            </div>\n          </else>\n        </for>\n      </div>\n    </div>\n  </if>\n</portal>\n\n<return=api>\n"
    },
    {
      "path": "ui/context-menu/submenu.marko",
      "type": "registry:file",
      "target": "~/src/components/ui/context-menu/submenu.marko",
      "content": "// One nested submenu: a real second `@zag-js/menu` machine instance, linked\n// to its parent menu's raw service via `setParent`/`setChild` — the wiring\n// zag's own connect() exposes for exactly this (`menu.connect(...).setParent`\n// / `.setChild`, both documented to accept the *raw* MarkoService, not the\n// connected api — confirmed against `@zag-js/menu@1.43.0`'s connect source).\n// Marko gives this its own component instance (and therefore its own\n// `<lifecycle>`) per call site, which is exactly the child-machine lifetime\n// zag wants — same shape as `<toast-item>`'s per-toast child service linked\n// to the toaster's group service. Copied from menubar/submenu.marko (see\n// that file's header for the full toast-precedent rationale) — this is the\n// same mechanical pattern, just context-menu's own item shape and class\n// strings.\n//\n// The trigger row lives in the PARENT menu's item list (it needs the\n// parent's `getTriggerItemProps(childApi)`, which only the parent's api\n// exposes), so this component renders only the sub-trigger's own subtree:\n// the trigger row itself (via `<@trigger>`, receiving this submenu's api so\n// the caller can call `getTriggerItemProps`) plus the popped-out content.\n// Rendered by `context-menu.marko` for `type: \"sub\"` entries. Supports one\n// level of nesting (a submenu's own items are flat — item/separator/label\n// only); a `type: \"sub\"` entry inside a submenu's `subEntries` is not wired.\nimport * as menu from \"@zag-js/menu\";\nimport { positionerStyle } from \"marko-zag\";\nimport Icon from \"../icon/icon.marko\";\nimport type { ContextMenuItem } from \"./context-menu.marko\";\n\nexport interface Input {\n  /** This submenu's own item list — same entry shape as the top-level menu. */\n  entries: ContextMenuItem[];\n  /** Label rendered on the sub-trigger row. */\n  label?: string;\n  disabled?: boolean;\n  inset?: boolean;\n  /** Getter for the parent menu's raw service — this submenu registers onto it via `setParent`. */\n  parent: () => menu.Service | null;\n  /**\n   * Fires once this submenu's own raw service exists (on mount), so the\n   * parent can complete the link with `parentApi().setChild(childService)` —\n   * `setChild` needs the RAW service (see menu.connect's `child.prop(\"id\")`),\n   * which only exists after this component's own `<service>` has mounted.\n   */\n  onChildReady?: (childService: menu.Service) => void;\n  /**\n   * Trigger render-prop: receives this submenu's connected api so the caller\n   * (the parent menu's item loop) can build the row with\n   * `parentApi().getTriggerItemProps(childApi)`.\n   */\n  trigger: Marko.AttrTag<{ content: Marko.Body<[menu.Api]> }>;\n}\n\n<machine-props/machineProps from=input pick=menu.props/>\n<service/service machine=() => menu.machine props=machineProps/>\n<connect/api=(service, normalizeProps) =>\n  menu.connect(service, normalizeProps)\n  service=service\n/>\n\n// Link this submenu to its parent as soon as both raw services exist.\n// `setParent`/`setChild` both take the raw MarkoService (see menu.connect's\n// `parent.prop(\"id\")` / `child.prop(\"id\")` calls) — `<service>`'s onMount has\n// already run for every ancestor by the time any descendant mounts (Marko\n// mounts render bottom-up after the full tree commits), so `input.parent()`\n// and `service.service` are both live here.\n<lifecycle\n  onMount() {\n    const parentService = input.parent();\n    const ownService = service.service;\n    if (!parentService || !ownService) return;\n    api().setParent(parentService);\n    input.onChildReady?.(ownService);\n  }\n/>\n\n<${input.trigger.content}(api())/>\n<portal>\n  <if=api().open>\n    <div ...api().getPositionerProps() style=positionerStyle data-slot=\"context-menu-sub-content-positioner\">\n      <div\n        ...api().getContentProps()\n        data-slot=\"context-menu-sub-content\"\n        class=\"data-open:animate-in data-closed:animate-out data-closed:fade-out-0 data-open:fade-in-0 data-closed:zoom-out-95 data-open:zoom-in-95 data-[side=bottom]:slide-in-from-top-2 data-[side=left]:slide-in-from-right-2 data-[side=right]:slide-in-from-left-2 data-[side=top]:slide-in-from-bottom-2 ring-foreground/10 bg-popover text-popover-foreground min-w-32 rounded-lg p-1 shadow-md ring-1 duration-100 mu-menu-target mu-menu-translucent z-50 origin-(--radix-context-menu-content-transform-origin) overflow-hidden\"\n      >\n        <for|entry, index| of=input.entries>\n          <const/value=entry.value ?? `sub-item-${index}`/>\n          <if=entry.type === \"separator\">\n            <div ...api().getSeparatorProps() data-slot=\"context-menu-separator\" class=\"bg-border/50 -mx-1 my-1 h-px\"/>\n          </if>\n          <else if=entry.type === \"label\">\n            <div data-slot=\"context-menu-label\" data-inset=entry.inset || undefined class=\"text-muted-foreground px-2 py-1.5 text-xs data-inset:pl-7.5\">\n              ${entry.label}\n            </div>\n          </else>\n          <else>\n            <div\n              ...api().getItemProps({ value, disabled: entry.disabled })\n              data-slot=\"context-menu-item\"\n              data-inset=entry.inset || undefined\n              data-variant=entry.variant ?? \"default\"\n              class=\"focus:bg-accent focus:text-accent-foreground data-[variant=destructive]:text-destructive data-[variant=destructive]:focus:bg-destructive/10 dark:data-[variant=destructive]:focus:bg-destructive/20 data-[variant=destructive]:focus:text-destructive data-[variant=destructive]:*:[svg]:text-destructive not-data-[variant=destructive]:focus:**:text-accent-foreground min-h-7 gap-2 rounded-md px-2 py-1 text-xs/relaxed data-inset:pl-7.5 [&_svg:not([class*='size-'])]:size-3.5 group/context-menu-item relative flex cursor-default items-center outline-hidden select-none data-disabled:pointer-events-none data-disabled:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0\"\n            >\n              ${entry.label}\n              <if=entry.shortcut>\n                <span data-slot=\"context-menu-shortcut\" class=\"text-muted-foreground group-focus/context-menu-item:text-accent-foreground ml-auto text-[0.625rem] tracking-widest\">${entry.shortcut}</span>\n              </if>\n            </div>\n          </else>\n        </for>\n      </div>\n    </div>\n  </if>\n</portal>\n"
    }
  ]
}