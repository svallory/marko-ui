{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "command",
  "type": "registry:ui",
  "title": "Command",
  "description": "Fast, composable, unstyled command menu for React.",
  "dependencies": [
    "@zag-js/combobox",
    "marko-zag"
  ],
  "registryDependencies": [
    "http://localhost:3000/r/utils.json",
    "http://localhost:3000/r/styles/mira/icon.json"
  ],
  "files": [
    {
      "path": "ui/command/command.marko",
      "type": "registry:file",
      "target": "~/src/components/ui/command/command.marko",
      "content": "import * as commandMachine from \"@zag-js/combobox\";\nimport type { MachineInput } from \"marko-zag\";\nimport Icon from \"../icon/icon.marko\";\nimport { cn } from \"../../../lib/utils.ts\";\nimport { stripOwnProps } from \"../../../lib/native-attrs.ts\";\n\n// Upstream's `CommandDialog` is a THIN separate wrapper function — not part\n// of `Command` itself — that composes `<Dialog><DialogContent\n// className=\"cn-command-dialog top-1/3 translate-y-0 overflow-hidden p-0\">\n// <Command/></DialogContent></Dialog>` with a sr-only title/description.\n// Dialog is being re-baselined concurrently by another agent (see\n// rebaseline-fleet-brief.md), so that composition is NOT wired up here to\n// avoid depending on its in-flight shape. Consumers get the same result by\n// composing it themselves against the shipped `Dialog` component:\n//\n//   <Dialog class=\"rounded-xl! top-1/3 translate-y-0 overflow-hidden p-0\" showCloseButton=false>\n//     <@title class=\"sr-only\">Command Palette</@title>\n//     <@description class=\"sr-only\">Search for a command to run...</@description>\n//     <@content><Command groups=... /></@content>\n//   </Dialog>\n//\n// `mu-command-dialog` is reserved here (this comment) for check-anchors\n// parity with upstream's `cn-command-dialog` hook — the style CSS layer\n// targets it regardless of which file composes the wrapper.\n\nexport interface CommandItem {\n  value: string;\n  label: string;\n  shortcut?: string;\n}\n\nexport interface CommandGroup {\n  label: string;\n  items: CommandItem[];\n}\n\n// `collection`, `value`, and `inputValue` are machine-owned in\n// combobox.Props but this component derives its own collection from\n// `groups` and manages the search query internally, so they are omitted\n// here rather than exposed on the public Input.\nexport type Input = Omit<\n  MachineInput<\"div\", commandMachine.Props>,\n  \"collection\" | \"value\" | \"inputValue\"\n> & {\n  groups: CommandGroup[];\n  valueChange?: (value: string) => void;\n};\n\nstatic function filterGroups(groups: CommandGroup[], query: string): CommandGroup[] {\n  const normalizedQuery = query.trim().toLowerCase();\n  if (!normalizedQuery) return groups;\n  return groups\n    .map((group) => ({\n      ...group,\n      items: group.items.filter((item) => item.label.toLowerCase().includes(normalizedQuery)),\n    }))\n    .filter((group) => group.items.length > 0);\n}\n\n<let/query=\"\"/>\n<const/filteredGroups=filterGroups(input.groups, query)/>\n<const/allItems=filteredGroups.flatMap((group) => group.items)/>\n\n// `collection` is machine-owned but derived from `groups`/`query`, not a\n// pass-through of `input`, so it is built here and merged into the props\n// closure below rather than passed through <machine-props>'s override\n// mechanism — overrides are copied as plain tag input, and a Zag collection\n// carries methods that would fail serialization if it crossed that boundary.\n<machine-props/machineProps from=input pick=commandMachine.props\n  open=true\n  inputBehavior=\"autohighlight\"\n  selectionBehavior=\"clear\"\n  onInputValueChange(details: commandMachine.InputValueChangeDetails) {\n    input.onInputValueChange?.(details);\n    query = details.inputValue;\n  }\n  onValueChange(details: commandMachine.ValueChangeDetails) {\n    input.onValueChange?.(details);\n    if (details.value[0] != null) {\n      input.valueChange?.(details.value[0]);\n    }\n  }/>\n<const/serviceProps=() => ({\n  ...machineProps(),\n  collection: commandMachine.collection({\n    items: allItems,\n    itemToValue: (item: CommandItem) => item.value,\n    itemToString: (item: CommandItem) => item.label,\n  }),\n})/>\n<service/service machine=() => commandMachine.machine props=serviceProps/>\n<connect/api=(service, normalizeProps) =>\n  commandMachine.connect(service, normalizeProps)\n  service=service\n/>\n\n<const/nativeAttrs=() =>\n  stripOwnProps(\n    commandMachine.splitProps(input)[1],\n    \"class\",\n    \"groups\",\n    \"valueChange\",\n  )\n>\n\n<div ...nativeAttrs() ...api().getRootProps() data-slot=\"command\" class=cn(\"bg-popover text-popover-foreground rounded-xl p-1 flex size-full flex-col overflow-hidden\", input.class)>\n  <div data-slot=\"command-input-wrapper\" class=\"p-1 pb-0\">\n    <div ...api().getControlProps() data-slot=\"command-input-group\" class=\"bg-input/20 dark:bg-input/30 h-8! group/input-group relative flex w-full min-w-0 items-center outline-none\">\n      <input\n        ...api().getInputProps()\n        data-slot=\"command-input\"\n        placeholder=input.placeholder\n        class=\"w-full text-xs/relaxed outline-hidden disabled:cursor-not-allowed disabled:opacity-50\"\n      />\n      <div data-slot=\"command-input-addon\" class=\"flex items-center\">\n        <Icon name=\"Search\" data-slot=\"command-input-icon\" class=\"size-3.5 shrink-0 opacity-50\"/>\n      </div>\n    </div>\n  </div>\n  <div ...api().getContentProps() data-slot=\"command-list\" class=\"no-scrollbar max-h-72 scroll-py-1 outline-none overflow-x-hidden overflow-y-auto\">\n    <if=(allItems.length === 0)>\n      <div data-slot=\"command-empty\" class=\"py-6 text-center text-xs/relaxed\">No results found.</div>\n    </if>\n    <else>\n      <for|group, groupIndex| of=filteredGroups>\n        <if=(groupIndex > 0)>\n          <div data-slot=\"command-separator\" class=\"bg-border/50 -mx-1 my-1 h-px\"></div>\n        </if>\n        <div ...api().getItemGroupProps({ id: `group-${groupIndex}` }) data-slot=\"command-group\" class=\"text-foreground **:[[cmdk-group-heading]]:text-muted-foreground overflow-hidden p-1 **:[[cmdk-group-heading]]:px-2.5 **:[[cmdk-group-heading]]:py-1.5 **:[[cmdk-group-heading]]:text-xs **:[[cmdk-group-heading]]:font-medium\">\n          <div ...api().getItemGroupLabelProps({ htmlFor: `group-${groupIndex}` }) cmdk-group-heading=\"\">\n            ${group.label}\n          </div>\n          <for|item| of=group.items>\n            <div\n              ...api().getItemProps({ item })\n              data-slot=\"command-item\"\n              class=\"data-selected:bg-muted data-selected:text-foreground data-selected:*:[svg]:text-foreground relative flex min-h-7 cursor-default items-center gap-2 rounded-md px-2.5 py-1.5 text-xs/relaxed outline-hidden select-none in-data-[slot=dialog-content]:rounded-md [&_svg:not([class*='size-'])]:size-3.5 group/command-item data-[disabled]:pointer-events-none data-[disabled]:opacity-50 [&_svg]:pointer-events-none [&_svg]:shrink-0 data-[highlighted]:bg-accent data-[highlighted]:text-accent-foreground\"\n            >\n              <span>${item.label}</span>\n              <if=item.shortcut>\n                <span data-slot=\"command-shortcut\" class=\"text-muted-foreground group-data-selected/command-item:text-foreground ml-auto text-[0.625rem] tracking-widest\">${item.shortcut}</span>\n              </if>\n              <Icon\n                name=\"Check\"\n                data-slot=\"command-item-indicator\"\n                class=\"ml-auto opacity-0 group-has-data-[slot=command-shortcut]/command-item:hidden group-data-[state=checked]/command-item:opacity-100\"\n              />\n            </div>\n          </for>\n        </div>\n      </for>\n    </else>\n  </div>\n</div>\n\n<return=api>\n"
    }
  ]
}