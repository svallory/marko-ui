// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the popover docs page: renders one of
// popover's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_popover_popover_alignments from "../../../demos/popover/popover-alignments.marko";
import Demo_popover_popover_basic from "../../../demos/popover/popover-basic.marko";
import Demo_popover_popover_controlled from "../../../demos/popover/popover-controlled.marko";
import Demo_popover_popover_demo from "../../../demos/popover/popover-demo.marko";
import Demo_popover_popover_form from "../../../demos/popover/popover-form.marko";
import Demo_popover_popover_placement from "../../../demos/popover/popover-placement.marko";
import Demo_popover_popover_rtl from "../../../demos/popover/popover-rtl.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "popover/popover-alignments". */
  demoId: string;
}

<if=(input.demoId === "popover/popover-alignments")><Demo_popover_popover_alignments/></if>
<else if=(input.demoId === "popover/popover-basic")><Demo_popover_popover_basic/></else>
<else if=(input.demoId === "popover/popover-controlled")><Demo_popover_popover_controlled/></else>
<else if=(input.demoId === "popover/popover-demo")><Demo_popover_popover_demo/></else>
<else if=(input.demoId === "popover/popover-form")><Demo_popover_popover_form/></else>
<else if=(input.demoId === "popover/popover-placement")><Demo_popover_popover_placement/></else>
<else if=(input.demoId === "popover/popover-rtl")><Demo_popover_popover_rtl/></else>
