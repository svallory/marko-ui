// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the marquee docs page: renders one of
// marquee's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_marquee_marquee_auto_fill from "../../../demos/marquee/marquee-auto-fill.marko";
import Demo_marquee_marquee_compound from "../../../demos/marquee/marquee-compound.marko";
import Demo_marquee_marquee_controlled from "../../../demos/marquee/marquee-controlled.marko";
import Demo_marquee_marquee_demo from "../../../demos/marquee/marquee-demo.marko";
import Demo_marquee_marquee_reverse from "../../../demos/marquee/marquee-reverse.marko";
import Demo_marquee_marquee_speed from "../../../demos/marquee/marquee-speed.marko";
import Demo_marquee_marquee_vertical from "../../../demos/marquee/marquee-vertical.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "marquee/marquee-auto-fill". */
  demoId: string;
}

<if=(input.demoId === "marquee/marquee-auto-fill")><Demo_marquee_marquee_auto_fill/></if>
<else if=(input.demoId === "marquee/marquee-compound")><Demo_marquee_marquee_compound/></else>
<else if=(input.demoId === "marquee/marquee-controlled")><Demo_marquee_marquee_controlled/></else>
<else if=(input.demoId === "marquee/marquee-demo")><Demo_marquee_marquee_demo/></else>
<else if=(input.demoId === "marquee/marquee-reverse")><Demo_marquee_marquee_reverse/></else>
<else if=(input.demoId === "marquee/marquee-speed")><Demo_marquee_marquee_speed/></else>
<else if=(input.demoId === "marquee/marquee-vertical")><Demo_marquee_marquee_vertical/></else>
