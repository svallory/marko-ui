// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the tour docs page: renders one of
// tour's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_tour_tour_controlled from "../../../demos/tour/tour-controlled.marko";
import Demo_tour_tour_demo from "../../../demos/tour/tour-demo.marko";
import Demo_tour_tour_sides from "../../../demos/tour/tour-sides.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "tour/tour-controlled". */
  demoId: string;
}

<if=(input.demoId === "tour/tour-controlled")><Demo_tour_tour_controlled/></if>
<else if=(input.demoId === "tour/tour-demo")><Demo_tour_tour_demo/></else>
<else if=(input.demoId === "tour/tour-sides")><Demo_tour_tour_sides/></else>
