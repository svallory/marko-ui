// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the marker docs page: renders one of
// marker's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_marker_marker_border from "../../../demos/marker/marker-border.marko";
import Demo_marker_marker_demo from "../../../demos/marker/marker-demo.marko";
import Demo_marker_marker_icon from "../../../demos/marker/marker-icon.marko";
import Demo_marker_marker_link_button from "../../../demos/marker/marker-link-button.marko";
import Demo_marker_marker_separator from "../../../demos/marker/marker-separator.marko";
import Demo_marker_marker_shimmer from "../../../demos/marker/marker-shimmer.marko";
import Demo_marker_marker_status from "../../../demos/marker/marker-status.marko";
import Demo_marker_marker_variants from "../../../demos/marker/marker-variants.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "marker/marker-border". */
  demoId: string;
}

<if=(input.demoId === "marker/marker-border")><Demo_marker_marker_border/></if>
<else if=(input.demoId === "marker/marker-demo")><Demo_marker_marker_demo/></else>
<else if=(input.demoId === "marker/marker-icon")><Demo_marker_marker_icon/></else>
<else if=(input.demoId === "marker/marker-link-button")><Demo_marker_marker_link_button/></else>
<else if=(input.demoId === "marker/marker-separator")><Demo_marker_marker_separator/></else>
<else if=(input.demoId === "marker/marker-shimmer")><Demo_marker_marker_shimmer/></else>
<else if=(input.demoId === "marker/marker-status")><Demo_marker_marker_status/></else>
<else if=(input.demoId === "marker/marker-variants")><Demo_marker_marker_variants/></else>
