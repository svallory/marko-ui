// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the attachment docs page: renders one of
// attachment's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_attachment_attachment_demo from "../../../demos/attachment/attachment-demo.marko";
import Demo_attachment_attachment_group from "../../../demos/attachment/attachment-group.marko";
import Demo_attachment_attachment_image from "../../../demos/attachment/attachment-image.marko";
import Demo_attachment_attachment_sizes from "../../../demos/attachment/attachment-sizes.marko";
import Demo_attachment_attachment_states from "../../../demos/attachment/attachment-states.marko";
import Demo_attachment_attachment_trigger from "../../../demos/attachment/attachment-trigger.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "attachment/attachment-demo". */
  demoId: string;
}

<if=(input.demoId === "attachment/attachment-demo")><Demo_attachment_attachment_demo/></if>
<else if=(input.demoId === "attachment/attachment-group")><Demo_attachment_attachment_group/></else>
<else if=(input.demoId === "attachment/attachment-image")><Demo_attachment_attachment_image/></else>
<else if=(input.demoId === "attachment/attachment-sizes")><Demo_attachment_attachment_sizes/></else>
<else if=(input.demoId === "attachment/attachment-states")><Demo_attachment_attachment_states/></else>
<else if=(input.demoId === "attachment/attachment-trigger")><Demo_attachment_attachment_trigger/></else>
