// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the sheet docs page: renders one of
// sheet's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_sheet_sheet_controlled from "../../../demos/sheet/sheet-controlled.marko";
import Demo_sheet_sheet_demo from "../../../demos/sheet/sheet-demo.marko";
import Demo_sheet_sheet_no_close_button from "../../../demos/sheet/sheet-no-close-button.marko";
import Demo_sheet_sheet_rtl from "../../../demos/sheet/sheet-rtl.marko";
import Demo_sheet_sheet_side from "../../../demos/sheet/sheet-side.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "sheet/sheet-controlled". */
  demoId: string;
}

<if=(input.demoId === "sheet/sheet-controlled")><Demo_sheet_sheet_controlled/></if>
<else if=(input.demoId === "sheet/sheet-demo")><Demo_sheet_sheet_demo/></else>
<else if=(input.demoId === "sheet/sheet-no-close-button")><Demo_sheet_sheet_no_close_button/></else>
<else if=(input.demoId === "sheet/sheet-rtl")><Demo_sheet_sheet_rtl/></else>
<else if=(input.demoId === "sheet/sheet-side")><Demo_sheet_sheet_side/></else>
