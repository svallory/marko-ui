// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the switch docs page: renders one of
// switch's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_switch_switch_checked from "../../../demos/switch/switch-checked.marko";
import Demo_switch_switch_choice_card from "../../../demos/switch/switch-choice-card.marko";
import Demo_switch_switch_controlled from "../../../demos/switch/switch-controlled.marko";
import Demo_switch_switch_demo from "../../../demos/switch/switch-demo.marko";
import Demo_switch_switch_description from "../../../demos/switch/switch-description.marko";
import Demo_switch_switch_disabled from "../../../demos/switch/switch-disabled.marko";
import Demo_switch_switch_invalid from "../../../demos/switch/switch-invalid.marko";
import Demo_switch_switch_rtl from "../../../demos/switch/switch-rtl.marko";
import Demo_switch_switch_sizes from "../../../demos/switch/switch-sizes.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "switch/switch-checked". */
  demoId: string;
}

<if=(input.demoId === "switch/switch-checked")><Demo_switch_switch_checked/></if>
<else if=(input.demoId === "switch/switch-choice-card")><Demo_switch_switch_choice_card/></else>
<else if=(input.demoId === "switch/switch-controlled")><Demo_switch_switch_controlled/></else>
<else if=(input.demoId === "switch/switch-demo")><Demo_switch_switch_demo/></else>
<else if=(input.demoId === "switch/switch-description")><Demo_switch_switch_description/></else>
<else if=(input.demoId === "switch/switch-disabled")><Demo_switch_switch_disabled/></else>
<else if=(input.demoId === "switch/switch-invalid")><Demo_switch_switch_invalid/></else>
<else if=(input.demoId === "switch/switch-rtl")><Demo_switch_switch_rtl/></else>
<else if=(input.demoId === "switch/switch-sizes")><Demo_switch_switch_sizes/></else>
