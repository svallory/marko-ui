// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the toggle docs page: renders one of
// toggle's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_toggle_toggle_controlled from "../../../demos/toggle/toggle-controlled.marko";
import Demo_toggle_toggle_demo from "../../../demos/toggle/toggle-demo.marko";
import Demo_toggle_toggle_disabled from "../../../demos/toggle/toggle-disabled.marko";
import Demo_toggle_toggle_outline from "../../../demos/toggle/toggle-outline.marko";
import Demo_toggle_toggle_rtl from "../../../demos/toggle/toggle-rtl.marko";
import Demo_toggle_toggle_sizes from "../../../demos/toggle/toggle-sizes.marko";
import Demo_toggle_toggle_text from "../../../demos/toggle/toggle-text.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "toggle/toggle-controlled". */
  demoId: string;
}

<if=(input.demoId === "toggle/toggle-controlled")><Demo_toggle_toggle_controlled/></if>
<else if=(input.demoId === "toggle/toggle-demo")><Demo_toggle_toggle_demo/></else>
<else if=(input.demoId === "toggle/toggle-disabled")><Demo_toggle_toggle_disabled/></else>
<else if=(input.demoId === "toggle/toggle-outline")><Demo_toggle_toggle_outline/></else>
<else if=(input.demoId === "toggle/toggle-rtl")><Demo_toggle_toggle_rtl/></else>
<else if=(input.demoId === "toggle/toggle-sizes")><Demo_toggle_toggle_sizes/></else>
<else if=(input.demoId === "toggle/toggle-text")><Demo_toggle_toggle_text/></else>
