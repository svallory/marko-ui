// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the timer docs page: renders one of
// timer's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_timer_timer_controlled from "../../../demos/timer/timer-controlled.marko";
import Demo_timer_timer_countdown from "../../../demos/timer/timer-countdown.marko";
import Demo_timer_timer_demo from "../../../demos/timer/timer-demo.marko";
import Demo_timer_timer_parts from "../../../demos/timer/timer-parts.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "timer/timer-controlled". */
  demoId: string;
}

<if=(input.demoId === "timer/timer-controlled")><Demo_timer_timer_controlled/></if>
<else if=(input.demoId === "timer/timer-countdown")><Demo_timer_timer_countdown/></else>
<else if=(input.demoId === "timer/timer-demo")><Demo_timer_timer_demo/></else>
<else if=(input.demoId === "timer/timer-parts")><Demo_timer_timer_parts/></else>
