// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the tabs docs page: renders one of
// tabs's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_tabs_tabs_compound from "../../../demos/tabs/tabs-compound.marko";
import Demo_tabs_tabs_controlled from "../../../demos/tabs/tabs-controlled.marko";
import Demo_tabs_tabs_demo from "../../../demos/tabs/tabs-demo.marko";
import Demo_tabs_tabs_disabled from "../../../demos/tabs/tabs-disabled.marko";
import Demo_tabs_tabs_hybrid from "../../../demos/tabs/tabs-hybrid.marko";
import Demo_tabs_tabs_icons from "../../../demos/tabs/tabs-icons.marko";
import Demo_tabs_tabs_line from "../../../demos/tabs/tabs-line.marko";
import Demo_tabs_tabs_precedence from "../../../demos/tabs/tabs-precedence.marko";
import Demo_tabs_tabs_rtl from "../../../demos/tabs/tabs-rtl.marko";
import Demo_tabs_tabs_vertical from "../../../demos/tabs/tabs-vertical.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "tabs/tabs-compound". */
  demoId: string;
}

<if=(input.demoId === "tabs/tabs-compound")><Demo_tabs_tabs_compound/></if>
<else if=(input.demoId === "tabs/tabs-controlled")><Demo_tabs_tabs_controlled/></else>
<else if=(input.demoId === "tabs/tabs-demo")><Demo_tabs_tabs_demo/></else>
<else if=(input.demoId === "tabs/tabs-disabled")><Demo_tabs_tabs_disabled/></else>
<else if=(input.demoId === "tabs/tabs-hybrid")><Demo_tabs_tabs_hybrid/></else>
<else if=(input.demoId === "tabs/tabs-icons")><Demo_tabs_tabs_icons/></else>
<else if=(input.demoId === "tabs/tabs-line")><Demo_tabs_tabs_line/></else>
<else if=(input.demoId === "tabs/tabs-precedence")><Demo_tabs_tabs_precedence/></else>
<else if=(input.demoId === "tabs/tabs-rtl")><Demo_tabs_tabs_rtl/></else>
<else if=(input.demoId === "tabs/tabs-vertical")><Demo_tabs_tabs_vertical/></else>
