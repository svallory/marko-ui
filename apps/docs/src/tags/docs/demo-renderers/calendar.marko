// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the calendar docs page: renders one of
// calendar's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_calendar_calendar_basic from "../../../demos/calendar/calendar-basic.marko";
import Demo_calendar_calendar_booked_dates from "../../../demos/calendar/calendar-booked-dates.marko";
import Demo_calendar_calendar_caption from "../../../demos/calendar/calendar-caption.marko";
import Demo_calendar_calendar_controlled from "../../../demos/calendar/calendar-controlled.marko";
import Demo_calendar_calendar_custom_days from "../../../demos/calendar/calendar-custom-days.marko";
import Demo_calendar_calendar_demo from "../../../demos/calendar/calendar-demo.marko";
import Demo_calendar_calendar_disabled from "../../../demos/calendar/calendar-disabled.marko";
import Demo_calendar_calendar_hijri from "../../../demos/calendar/calendar-hijri.marko";
import Demo_calendar_calendar_min_max from "../../../demos/calendar/calendar-min-max.marko";
import Demo_calendar_calendar_multiple from "../../../demos/calendar/calendar-multiple.marko";
import Demo_calendar_calendar_presets from "../../../demos/calendar/calendar-presets.marko";
import Demo_calendar_calendar_range from "../../../demos/calendar/calendar-range.marko";
import Demo_calendar_calendar_rtl from "../../../demos/calendar/calendar-rtl.marko";
import Demo_calendar_calendar_time from "../../../demos/calendar/calendar-time.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "calendar/calendar-basic". */
  demoId: string;
}

<if=(input.demoId === "calendar/calendar-basic")><Demo_calendar_calendar_basic/></if>
<else if=(input.demoId === "calendar/calendar-booked-dates")><Demo_calendar_calendar_booked_dates/></else>
<else if=(input.demoId === "calendar/calendar-caption")><Demo_calendar_calendar_caption/></else>
<else if=(input.demoId === "calendar/calendar-controlled")><Demo_calendar_calendar_controlled/></else>
<else if=(input.demoId === "calendar/calendar-custom-days")><Demo_calendar_calendar_custom_days/></else>
<else if=(input.demoId === "calendar/calendar-demo")><Demo_calendar_calendar_demo/></else>
<else if=(input.demoId === "calendar/calendar-disabled")><Demo_calendar_calendar_disabled/></else>
<else if=(input.demoId === "calendar/calendar-hijri")><Demo_calendar_calendar_hijri/></else>
<else if=(input.demoId === "calendar/calendar-min-max")><Demo_calendar_calendar_min_max/></else>
<else if=(input.demoId === "calendar/calendar-multiple")><Demo_calendar_calendar_multiple/></else>
<else if=(input.demoId === "calendar/calendar-presets")><Demo_calendar_calendar_presets/></else>
<else if=(input.demoId === "calendar/calendar-range")><Demo_calendar_calendar_range/></else>
<else if=(input.demoId === "calendar/calendar-rtl")><Demo_calendar_calendar_rtl/></else>
<else if=(input.demoId === "calendar/calendar-time")><Demo_calendar_calendar_time/></else>
