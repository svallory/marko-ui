// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the steps docs page: renders one of
// steps's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_steps_steps_controlled from "../../../demos/steps/steps-controlled.marko";
import Demo_steps_steps_horizontal from "../../../demos/steps/steps-horizontal.marko";
import Demo_steps_steps_linear from "../../../demos/steps/steps-linear.marko";
import Demo_steps_steps_vertical from "../../../demos/steps/steps-vertical.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "steps/steps-controlled". */
  demoId: string;
}

<if=(input.demoId === "steps/steps-controlled")><Demo_steps_steps_controlled/></if>
<else if=(input.demoId === "steps/steps-horizontal")><Demo_steps_steps_horizontal/></else>
<else if=(input.demoId === "steps/steps-linear")><Demo_steps_steps_linear/></else>
<else if=(input.demoId === "steps/steps-vertical")><Demo_steps_steps_vertical/></else>
