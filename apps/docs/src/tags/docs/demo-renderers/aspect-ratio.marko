// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the aspect-ratio docs page: renders one of
// aspect-ratio's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_aspect_ratio_aspect_ratio_content_card from "../../../demos/aspect-ratio/aspect-ratio-content-card.marko";
import Demo_aspect_ratio_aspect_ratio_demo from "../../../demos/aspect-ratio/aspect-ratio-demo.marko";
import Demo_aspect_ratio_aspect_ratio_portrait from "../../../demos/aspect-ratio/aspect-ratio-portrait.marko";
import Demo_aspect_ratio_aspect_ratio_rtl from "../../../demos/aspect-ratio/aspect-ratio-rtl.marko";
import Demo_aspect_ratio_aspect_ratio_square from "../../../demos/aspect-ratio/aspect-ratio-square.marko";
import Demo_aspect_ratio_aspect_ratio_ultrawide_banner from "../../../demos/aspect-ratio/aspect-ratio-ultrawide-banner.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "aspect-ratio/aspect-ratio-content-card". */
  demoId: string;
}

<if=(input.demoId === "aspect-ratio/aspect-ratio-content-card")><Demo_aspect_ratio_aspect_ratio_content_card/></if>
<else if=(input.demoId === "aspect-ratio/aspect-ratio-demo")><Demo_aspect_ratio_aspect_ratio_demo/></else>
<else if=(input.demoId === "aspect-ratio/aspect-ratio-portrait")><Demo_aspect_ratio_aspect_ratio_portrait/></else>
<else if=(input.demoId === "aspect-ratio/aspect-ratio-rtl")><Demo_aspect_ratio_aspect_ratio_rtl/></else>
<else if=(input.demoId === "aspect-ratio/aspect-ratio-square")><Demo_aspect_ratio_aspect_ratio_square/></else>
<else if=(input.demoId === "aspect-ratio/aspect-ratio-ultrawide-banner")><Demo_aspect_ratio_aspect_ratio_ultrawide_banner/></else>
