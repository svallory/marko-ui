// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the image-cropper docs page: renders one of
// image-cropper's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_image_cropper_image_cropper_circle_aspect_ratio from "../../../demos/image-cropper/image-cropper-circle-aspect-ratio.marko";
import Demo_image_cropper_image_cropper_controlled from "../../../demos/image-cropper/image-cropper-controlled.marko";
import Demo_image_cropper_image_cropper_demo from "../../../demos/image-cropper/image-cropper-demo.marko";
import Demo_image_cropper_image_cropper_fixed_crop_area from "../../../demos/image-cropper/image-cropper-fixed-crop-area.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "image-cropper/image-cropper-circle-aspect-ratio". */
  demoId: string;
}

<if=(input.demoId === "image-cropper/image-cropper-circle-aspect-ratio")><Demo_image_cropper_image_cropper_circle_aspect_ratio/></if>
<else if=(input.demoId === "image-cropper/image-cropper-controlled")><Demo_image_cropper_image_cropper_controlled/></else>
<else if=(input.demoId === "image-cropper/image-cropper-demo")><Demo_image_cropper_image_cropper_demo/></else>
<else if=(input.demoId === "image-cropper/image-cropper-fixed-crop-area")><Demo_image_cropper_image_cropper_fixed_crop_area/></else>
