// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the pagination docs page: renders one of
// pagination's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_pagination_pagination_controlled from "../../../demos/pagination/pagination-controlled.marko";
import Demo_pagination_pagination_demo from "../../../demos/pagination/pagination-demo.marko";
import Demo_pagination_pagination_icons_only from "../../../demos/pagination/pagination-icons-only.marko";
import Demo_pagination_pagination_rtl from "../../../demos/pagination/pagination-rtl.marko";
import Demo_pagination_pagination_sibling_count from "../../../demos/pagination/pagination-sibling-count.marko";
import Demo_pagination_pagination_simple from "../../../demos/pagination/pagination-simple.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "pagination/pagination-controlled". */
  demoId: string;
}

<if=(input.demoId === "pagination/pagination-controlled")><Demo_pagination_pagination_controlled/></if>
<else if=(input.demoId === "pagination/pagination-demo")><Demo_pagination_pagination_demo/></else>
<else if=(input.demoId === "pagination/pagination-icons-only")><Demo_pagination_pagination_icons_only/></else>
<else if=(input.demoId === "pagination/pagination-rtl")><Demo_pagination_pagination_rtl/></else>
<else if=(input.demoId === "pagination/pagination-sibling-count")><Demo_pagination_pagination_sibling_count/></else>
<else if=(input.demoId === "pagination/pagination-simple")><Demo_pagination_pagination_simple/></else>
