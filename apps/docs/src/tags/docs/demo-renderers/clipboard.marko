// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the clipboard docs page: renders one of
// clipboard's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_clipboard_clipboard_controlled from "../../../demos/clipboard/clipboard-controlled.marko";
import Demo_clipboard_clipboard_demo from "../../../demos/clipboard/clipboard-demo.marko";
import Demo_clipboard_clipboard_disabled from "../../../demos/clipboard/clipboard-disabled.marko";
import Demo_clipboard_clipboard_timeout from "../../../demos/clipboard/clipboard-timeout.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "clipboard/clipboard-controlled". */
  demoId: string;
}

<if=(input.demoId === "clipboard/clipboard-controlled")><Demo_clipboard_clipboard_controlled/></if>
<else if=(input.demoId === "clipboard/clipboard-demo")><Demo_clipboard_clipboard_demo/></else>
<else if=(input.demoId === "clipboard/clipboard-disabled")><Demo_clipboard_clipboard_disabled/></else>
<else if=(input.demoId === "clipboard/clipboard-timeout")><Demo_clipboard_clipboard_timeout/></else>
