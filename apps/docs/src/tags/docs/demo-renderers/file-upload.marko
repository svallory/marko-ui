// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the file-upload docs page: renders one of
// file-upload's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_file_upload_file_upload_accept from "../../../demos/file-upload/file-upload-accept.marko";
import Demo_file_upload_file_upload_controlled from "../../../demos/file-upload/file-upload-controlled.marko";
import Demo_file_upload_file_upload_demo from "../../../demos/file-upload/file-upload-demo.marko";
import Demo_file_upload_file_upload_max_size from "../../../demos/file-upload/file-upload-max-size.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "file-upload/file-upload-accept". */
  demoId: string;
}

<if=(input.demoId === "file-upload/file-upload-accept")><Demo_file_upload_file_upload_accept/></if>
<else if=(input.demoId === "file-upload/file-upload-controlled")><Demo_file_upload_file_upload_controlled/></else>
<else if=(input.demoId === "file-upload/file-upload-demo")><Demo_file_upload_file_upload_demo/></else>
<else if=(input.demoId === "file-upload/file-upload-max-size")><Demo_file_upload_file_upload_max_size/></else>
