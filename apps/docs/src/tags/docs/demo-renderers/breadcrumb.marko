// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the breadcrumb docs page: renders one of
// breadcrumb's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_breadcrumb_breadcrumb_basic from "../../../demos/breadcrumb/breadcrumb-basic.marko";
import Demo_breadcrumb_breadcrumb_demo from "../../../demos/breadcrumb/breadcrumb-demo.marko";
import Demo_breadcrumb_breadcrumb_dropdown from "../../../demos/breadcrumb/breadcrumb-dropdown.marko";
import Demo_breadcrumb_breadcrumb_ellipsis from "../../../demos/breadcrumb/breadcrumb-ellipsis.marko";
import Demo_breadcrumb_breadcrumb_link from "../../../demos/breadcrumb/breadcrumb-link.marko";
import Demo_breadcrumb_breadcrumb_responsive from "../../../demos/breadcrumb/breadcrumb-responsive.marko";
import Demo_breadcrumb_breadcrumb_rtl from "../../../demos/breadcrumb/breadcrumb-rtl.marko";
import Demo_breadcrumb_breadcrumb_separator from "../../../demos/breadcrumb/breadcrumb-separator.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "breadcrumb/breadcrumb-basic". */
  demoId: string;
}

<if=(input.demoId === "breadcrumb/breadcrumb-basic")><Demo_breadcrumb_breadcrumb_basic/></if>
<else if=(input.demoId === "breadcrumb/breadcrumb-demo")><Demo_breadcrumb_breadcrumb_demo/></else>
<else if=(input.demoId === "breadcrumb/breadcrumb-dropdown")><Demo_breadcrumb_breadcrumb_dropdown/></else>
<else if=(input.demoId === "breadcrumb/breadcrumb-ellipsis")><Demo_breadcrumb_breadcrumb_ellipsis/></else>
<else if=(input.demoId === "breadcrumb/breadcrumb-link")><Demo_breadcrumb_breadcrumb_link/></else>
<else if=(input.demoId === "breadcrumb/breadcrumb-responsive")><Demo_breadcrumb_breadcrumb_responsive/></else>
<else if=(input.demoId === "breadcrumb/breadcrumb-rtl")><Demo_breadcrumb_breadcrumb_rtl/></else>
<else if=(input.demoId === "breadcrumb/breadcrumb-separator")><Demo_breadcrumb_breadcrumb_separator/></else>
