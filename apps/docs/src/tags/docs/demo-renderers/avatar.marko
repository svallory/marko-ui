// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the avatar docs page: renders one of
// avatar's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_avatar_avatar_badge from "../../../demos/avatar/avatar-badge.marko";
import Demo_avatar_avatar_badge_icon from "../../../demos/avatar/avatar-badge-icon.marko";
import Demo_avatar_avatar_basic from "../../../demos/avatar/avatar-basic.marko";
import Demo_avatar_avatar_broken_image from "../../../demos/avatar/avatar-broken-image.marko";
import Demo_avatar_avatar_controlled from "../../../demos/avatar/avatar-controlled.marko";
import Demo_avatar_avatar_custom_fallback from "../../../demos/avatar/avatar-custom-fallback.marko";
import Demo_avatar_avatar_demo from "../../../demos/avatar/avatar-demo.marko";
import Demo_avatar_avatar_dropdown from "../../../demos/avatar/avatar-dropdown.marko";
import Demo_avatar_avatar_fallback from "../../../demos/avatar/avatar-fallback.marko";
import Demo_avatar_avatar_group from "../../../demos/avatar/avatar-group.marko";
import Demo_avatar_avatar_group_count from "../../../demos/avatar/avatar-group-count.marko";
import Demo_avatar_avatar_group_count_icon from "../../../demos/avatar/avatar-group-count-icon.marko";
import Demo_avatar_avatar_rtl from "../../../demos/avatar/avatar-rtl.marko";
import Demo_avatar_avatar_size from "../../../demos/avatar/avatar-size.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "avatar/avatar-badge". */
  demoId: string;
}

<if=(input.demoId === "avatar/avatar-badge")><Demo_avatar_avatar_badge/></if>
<else if=(input.demoId === "avatar/avatar-badge-icon")><Demo_avatar_avatar_badge_icon/></else>
<else if=(input.demoId === "avatar/avatar-basic")><Demo_avatar_avatar_basic/></else>
<else if=(input.demoId === "avatar/avatar-broken-image")><Demo_avatar_avatar_broken_image/></else>
<else if=(input.demoId === "avatar/avatar-controlled")><Demo_avatar_avatar_controlled/></else>
<else if=(input.demoId === "avatar/avatar-custom-fallback")><Demo_avatar_avatar_custom_fallback/></else>
<else if=(input.demoId === "avatar/avatar-demo")><Demo_avatar_avatar_demo/></else>
<else if=(input.demoId === "avatar/avatar-dropdown")><Demo_avatar_avatar_dropdown/></else>
<else if=(input.demoId === "avatar/avatar-fallback")><Demo_avatar_avatar_fallback/></else>
<else if=(input.demoId === "avatar/avatar-group")><Demo_avatar_avatar_group/></else>
<else if=(input.demoId === "avatar/avatar-group-count")><Demo_avatar_avatar_group_count/></else>
<else if=(input.demoId === "avatar/avatar-group-count-icon")><Demo_avatar_avatar_group_count_icon/></else>
<else if=(input.demoId === "avatar/avatar-rtl")><Demo_avatar_avatar_rtl/></else>
<else if=(input.demoId === "avatar/avatar-size")><Demo_avatar_avatar_size/></else>
