// GENERATED by apps/docs/scripts/build-demos-manifest.ts — do not edit.
// Regenerate after adding or changing anything under src/demos/:
//   bun apps/docs/scripts/build-demos-manifest.ts
//
// Static-dispatch tag for the resizable docs page: renders one of
// resizable's demos, chosen by a DemoEntry.demoId from
// demos-manifest.ts, via the static chain below. Never a dynamic tag over a
// runtime component lookup — Marko 6 crashes client hydration in production
// builds for those (notes/bug-marko-dynamic-tag-hydration-crash.md).
import Demo_resizable_resizable_controlled from "../../../demos/resizable/resizable-controlled.marko";
import Demo_resizable_resizable_demo from "../../../demos/resizable/resizable-demo.marko";
import Demo_resizable_resizable_handle from "../../../demos/resizable/resizable-handle.marko";
import Demo_resizable_resizable_rtl from "../../../demos/resizable/resizable-rtl.marko";
import Demo_resizable_resizable_vertical from "../../../demos/resizable/resizable-vertical.marko";

export interface Input {
  /** A DemoEntry.demoId from demos-manifest.ts, e.g. "resizable/resizable-controlled". */
  demoId: string;
}

<if=(input.demoId === "resizable/resizable-controlled")><Demo_resizable_resizable_controlled/></if>
<else if=(input.demoId === "resizable/resizable-demo")><Demo_resizable_resizable_demo/></else>
<else if=(input.demoId === "resizable/resizable-handle")><Demo_resizable_resizable_handle/></else>
<else if=(input.demoId === "resizable/resizable-rtl")><Demo_resizable_resizable_rtl/></else>
<else if=(input.demoId === "resizable/resizable-vertical")><Demo_resizable_resizable_vertical/></else>
