// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: upstream's resizable-rtl demo drives a live docs-site
// language switcher (useTranslation across en/ar/he) that has no
// equivalent in this app — same established precedent as
// demos/tabs/tabs-rtl.marko, demos/separator/separator-rtl.marko, and
// demos/accordion/accordion-rtl.marko. This ports the RTL VISUAL and
// markup directly using the source's default locale ("ar"): a
// `dir="rtl"` wrapper around the same nested-panels composition as
// resizable-demo, with Arabic labels. Resizable's layout is Tailwind flex
// with orientation read off a `data-orientation` attribute (no hard-coded
// physical left/right offsets on the panel group itself), so it restyles
// correctly under dir="rtl" with no component changes needed.
import Resizable from "@marko-ui/shadcn/ui/resizable/resizable.marko";

static const OUTER_PANELS = [
  { id: "one", defaultSize: 50 },
  { id: "two", defaultSize: 50 },
];

static const INNER_PANELS = [
  { id: "two-a", defaultSize: 25 },
  { id: "two-b", defaultSize: 75 },
];

<Resizable|panel| panels=OUTER_PANELS class="max-w-sm rounded-lg border" dir="rtl">
  <if=panel.id === "one">
    <div class="flex h-[200px] items-center justify-center p-6">
      <span class="font-semibold">واحد</span>
    </div>
  </if>
  <else>
    <Resizable|innerPanel| panels=INNER_PANELS orientation="vertical" dir="rtl">
      <div class="flex h-full items-center justify-center p-6">
        <span class="font-semibold">${innerPanel.id === "two-a" ? "اثنان" : "ثلاثة"}</span>
      </div>
    </Resizable>
  </else>
</Resizable>
