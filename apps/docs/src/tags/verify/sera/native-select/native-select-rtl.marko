// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: shadcn's source drives this demo through a client-side
// language-selector (useTranslation) that has no equivalent in this docs
// app (see skills/port-to-marko — no such utility exists here; established
// precedent: demos/combobox/combobox-rtl.marko, demos/select/select-rtl.marko).
// Instead of faking an interactive language switcher, this ports the RTL
// VISUAL and markup behavior directly: a `dir="rtl"` select with Arabic
// copy, matching the source's default locale ("ar"). Unlike select/combobox,
// native-select wraps a plain HTML `<select>` with no Zag machine — `dir` is
// not a component-owned prop, so it passes straight through native-select.marko's
// native-attribute forwarding onto the `<select>` element, where the browser's
// own RTL rendering and keyboard handling take over natively.
import NativeSelect from "@marko-ui/shadcn/ui/native-select/native-select.marko";
import NativeSelectOption from "@marko-ui/shadcn/ui/native-select/native-select-option.marko";

<NativeSelect dir="rtl" aria-label="اختر الحالة">
  <NativeSelectOption value="">اختر الحالة</NativeSelectOption>
  <NativeSelectOption value="todo">مهام</NativeSelectOption>
  <NativeSelectOption value="in-progress">قيد التنفيذ</NativeSelectOption>
  <NativeSelectOption value="done">منجز</NativeSelectOption>
  <NativeSelectOption value="cancelled">ملغي</NativeSelectOption>
</NativeSelect>
