// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: shadcn's source drives this demo through a client-side
// language-selector (useTranslation) that has no equivalent in this docs
// app (see skills/port-to-marko — no such utility exists here). Instead of
// faking an interactive language switcher, this ports the RTL VISUAL and
// markup behavior directly: a `dir="rtl"` field with Arabic copy, matching
// the source's default locale ("ar"). Field/Input already restyle correctly
// under `dir="rtl"` since their layout is Tailwind logical properties.
import Field from "@marko-ui/shadcn/ui/field/field.marko";
import FieldDescription from "@marko-ui/shadcn/ui/field/field-description.marko";
import FieldLabel from "@marko-ui/shadcn/ui/field/field-label.marko";
import Input from "@marko-ui/shadcn/ui/input/input.marko";

<Field dir="rtl">
  <FieldLabel for="input-rtl-api-key">مفتاح API</FieldLabel>
  <Input id="input-rtl-api-key" type="password" placeholder="sk-..." dir="rtl"/>
  <FieldDescription>مفتاح API الخاص بك مشفر ومخزن بأمان.</FieldDescription>
</Field>
