// GENERATED by build-verify-matrix.ts — do not edit
import ToggleGroup from "@marko-ui/shadcn/ui/toggle-group/toggle-group.marko";

// Static RTL port: upstream's toggle-group-rtl demo drives a live docs-site
// language switcher (useTranslation across en/ar/he) that has no
// equivalent in this app — same established precedent as
// demos/accordion/accordion-rtl.marko, demos/checkbox/checkbox-rtl.marko,
// and demos/input/input-rtl.marko. This ports the RTL VISUAL and behavior
// directly: static Arabic content (the source's default locale) with
// `dir="rtl"` passed straight through to the machine, which drives the
// Zag toggle-group's own RTL-aware layout/keyboard handling — not just a
// wrapping `dir` attribute.
<ToggleGroup variant="outline" value=["list"] dir="rtl">
  <@item value="list">قائمة</@item>
  <@item value="grid">شبكة</@item>
  <@item value="cards">بطاقات</@item>
</ToggleGroup>
