// GENERATED by build-verify-matrix.ts — do not edit
import Accordion from "@marko-ui/shadcn/ui/accordion/accordion.marko";

// Static RTL port: upstream's accordion-rtl demo drives a live docs-site
// language switcher (useTranslation across en/ar/he) that has no
// equivalent in this app — same established precedent as
// demos/card/card-rtl.marko, demos/tooltip/tooltip-rtl.marko, and
// demos/input/input-rtl.marko. This ports the RTL VISUAL and markup
// directly using the source's default locale ("ar"): a `dir="rtl"`
// accordion with Arabic copy. `dir` IS a real zag AccordionProps field
// (inherited via DirectionProperty in @zag-js/types, and present in the
// runtime `accordionMachine.props` list — see
// accordion.props.mjs/accordion.connect.mjs), so it is picked up by
// `<zag>`'s `from=input` (which selects by `accordionMachine.props`) and
// wired into the machine; `getRootProps()` then re-emits it as the root's
// `dir` attribute and `prop("dir")` correctly flips ArrowLeft/ArrowRight for
// keyboard nav. No component change is needed, but the reason is that
// `dir` already flows through the existing `<zag>` pick — not that
// it bypasses the machine as a plain native attribute.
static const items = [
  {
    value: "item-1",
    title: "كيف يمكنني إعادة تعيين كلمة المرور؟",
    content:
      "انقر على 'نسيت كلمة المرور' في صفحة تسجيل الدخول، أدخل عنوان بريدك الإلكتروني، وسنرسل لك رابطًا لإعادة تعيين كلمة المرور. سينتهي صلاحية الرابط خلال 24 ساعة.",
  },
  {
    value: "item-2",
    title: "هل يمكنني تغيير خطة الاشتراك الخاصة بي؟",
    content:
      "نعم، يمكنك ترقية أو تخفيض خطتك في أي وقت من إعدادات حسابك. ستظهر التغييرات في دورة الفوترة التالية.",
  },
  {
    value: "item-3",
    title: "ما هي طرق الدفع التي تقبلونها؟",
    content:
      "نقبل جميع بطاقات الائتمان الرئيسية و PayPal والتحويلات المصرفية. تتم معالجة جميع المدفوعات بأمان من خلال شركاء الدفع لدينا.",
  },
];

<Accordion items=items defaultValue=["item-1"] dir="rtl" class="max-w-md"/>
