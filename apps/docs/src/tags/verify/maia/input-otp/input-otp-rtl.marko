// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: shadcn's source drives this demo through a client-side
// language-selector (useTranslation) that has no equivalent in this docs
// app — see demos/input/input-rtl.marko for the established precedent.
// This ports the RTL VISUAL and behavior directly: a `dir="rtl"` field
// with Arabic copy, matching the source's default locale ("ar"). The
// machine's own `dir` prop (picked from `pinInputMachine.props`) drives
// the slot layout and arrow-key navigation direction; Field's layout is
// Tailwind logical properties, so it already restyles correctly.
import InputOTP from "@marko-ui/shadcn/ui/input-otp/input-otp.marko";
import Field from "@marko-ui/shadcn/ui/field/field.marko";
import FieldLabel from "@marko-ui/shadcn/ui/field/field-label.marko";

<Field class="mx-auto max-w-xs">
  <FieldLabel for="input-otp-rtl">رمز التحقق</FieldLabel>
  <InputOTP id="input-otp-rtl" length=6 defaultValue=["1","2","3","4","5","6"] dir="rtl"/>
</Field>
