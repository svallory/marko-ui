// GENERATED by build-verify-matrix.ts — do not edit
import InputOTP from "@marko-ui/shadcn/ui/input-otp/input-otp.marko";
import Field from "@marko-ui/shadcn/ui/field/field.marko";
import FieldLabel from "@marko-ui/shadcn/ui/field/field-label.marko";

// Upstream imports the `REGEXP_ONLY_DIGITS` constant from the `input-otp`
// npm package (a React-only dependency we don't take). Our machine's
// `pattern` prop takes the regex SOURCE as a plain string, so the constant
// is inlined here instead of imported.
<Field class="w-fit">
  <FieldLabel for="digits-only">Digits Only</FieldLabel>
  <InputOTP id="digits-only" length=6 pattern="^\d+$"/>
</Field>
