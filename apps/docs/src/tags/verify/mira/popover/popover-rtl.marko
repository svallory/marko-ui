// GENERATED by build-verify-matrix.ts — do not edit
import Popover from "@marko-ui/shadcn/ui/popover/popover.marko";
import Button from "@marko-ui/shadcn/ui/button/button.marko";

// Upstream's popover-rtl demo drives a live language switcher
// (useTranslation) with no equivalent in this docs app — see
// demos/tooltip/tooltip-rtl.marko for the established precedent. This
// ports the RTL VISUAL and behavior directly: a dir="rtl" row with Arabic
// copy, matching the source's default locale ("ar"). Upstream's `side`
// prop (left/top/bottom/right) maps directly onto Zag's
// positioning.placement — floating-ui auto-flips left/right placements
// under dir="rtl", the actual behavior the upstream demo showcases.
<div dir="rtl" class="flex flex-wrap justify-center gap-2">
  <Popover dir="rtl" positioning={ placement: "left" }>
    <@trigger|props|>
      <Button ...props variant="outline">يسار</Button>
    </@trigger>
    <@content>
      <div class="grid gap-2">
        <h4 class="leading-none font-medium">الأبعاد</h4>
        <p class="text-muted-foreground text-sm">تعيين الأبعاد للطبقة.</p>
      </div>
    </@content>
  </Popover>
  <Popover dir="rtl" positioning={ placement: "top" }>
    <@trigger|props|>
      <Button ...props variant="outline">أعلى</Button>
    </@trigger>
    <@content>
      <div class="grid gap-2">
        <h4 class="leading-none font-medium">الأبعاد</h4>
        <p class="text-muted-foreground text-sm">تعيين الأبعاد للطبقة.</p>
      </div>
    </@content>
  </Popover>
  <Popover dir="rtl" positioning={ placement: "bottom" }>
    <@trigger|props|>
      <Button ...props variant="outline">أسفل</Button>
    </@trigger>
    <@content>
      <div class="grid gap-2">
        <h4 class="leading-none font-medium">الأبعاد</h4>
        <p class="text-muted-foreground text-sm">تعيين الأبعاد للطبقة.</p>
      </div>
    </@content>
  </Popover>
  <Popover dir="rtl" positioning={ placement: "right" }>
    <@trigger|props|>
      <Button ...props variant="outline">يمين</Button>
    </@trigger>
    <@content>
      <div class="grid gap-2">
        <h4 class="leading-none font-medium">الأبعاد</h4>
        <p class="text-muted-foreground text-sm">تعيين الأبعاد للطبقة.</p>
      </div>
    </@content>
  </Popover>
</div>
