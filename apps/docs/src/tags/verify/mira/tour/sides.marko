// GENERATED by build-verify-matrix.ts — do not edit
import Tour from "@marko-ui/registry/styles/mira/ui/tour/tour.marko";
import Button from "@marko-ui/registry/styles/mira/ui/button/button.marko";

static const STEPS = [
  {
    id: "filters",
    title: "Filters",
    description: "Narrow the list down to what matters right now.",
    target: "#tour-filters",
    placement: "right",
    arrow: true,
    actions: [{ label: "Next", action: "next" }],
  },
  {
    id: "export",
    title: "Export",
    description: "Download the current view as CSV.",
    target: "#tour-export",
    placement: "left",
    arrow: true,
    actions: [
      { label: "Back", action: "prev" },
      { label: "Done", action: "dismiss" },
    ],
  },
];

<div class="flex w-full flex-col gap-4">
  <div class="flex items-center gap-3">
    <button
      id="tour-filters"
      class="border-input bg-background hover:bg-accent h-9 rounded-md border px-3 text-sm"
    >
      Filters
    </button>
    <button
      id="tour-export"
      class="border-input bg-background hover:bg-accent h-9 rounded-md border px-3 text-sm"
    >
      Export
    </button>
  </div>
  <div>
    <Tour items=STEPS>
      <@trigger|props|>
        <Button ...props id="start-short-tour" variant="outline">
          Start short tour
        </Button>
      </@trigger>
    </Tour>
  </div>
</div>
