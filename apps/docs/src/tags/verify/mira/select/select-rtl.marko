// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: shadcn's source drives this demo through a client-side
// language-selector (useTranslation) that has no equivalent in this docs
// app (see skills/port-to-marko — no such utility exists here; established
// precedent: demos/combobox/combobox-rtl.marko). Instead of faking an
// interactive language switcher, this ports the RTL VISUAL and markup
// behavior directly: a `dir="rtl"` select with Arabic copy, matching the
// source's default locale ("ar"). `dir` is a real machine prop
// (@zag-js/select `select.props`), so it drives both the DOM attribute and
// Zag's own RTL-aware keyboard/positioning logic.
import Select from "@marko-ui/shadcn/ui/select/select.marko";

static const groups = [
  {
    label: "الفواكه",
    items: [
      { value: "apple", label: "تفاح" },
      { value: "banana", label: "موز" },
      { value: "blueberry", label: "توت أزرق" },
      { value: "grapes", label: "عنب" },
      { value: "pineapple", label: "أناناس" },
    ],
  },
  {
    label: "الخضروات",
    items: [
      { value: "carrot", label: "جزر" },
      { value: "broccoli", label: "بروكلي" },
      { value: "spinach", label: "سبانخ" },
    ],
  },
];

<Select groups=groups placeholder="اختر فاكهة" dir="rtl" class="w-32"/>
