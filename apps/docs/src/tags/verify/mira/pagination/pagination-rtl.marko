// GENERATED by build-verify-matrix.ts — do not edit
import Pagination from "@marko-ui/shadcn/ui/pagination/pagination.marko";

// Static RTL port: shadcn's source drives this demo through a client-side
// language selector (useTranslation, cycling en/ar/he) that has no
// equivalent in this docs app — see demos/input/input-rtl.marko and
// demos/field/field-rtl.marko for the established precedent. This ports the
// RTL VISUAL directly: `dir="rtl"` on the nav, matching the source's
// default locale ("ar"). Pagination's icons already carry `mu-rtl-flip` so
// the chevrons mirror correctly under RTL with no component changes.
//
// Deviation from upstream: shadcn's Previous/Next buttons take a `text`
// prop so the RTL demo can swap in Arabic labels ("السابق"/"التالي"). Our
// Pagination hard-codes the English "Previous"/"Next" strings (no `text`
// prop) — labels stay in English here; only direction/layout mirror. Page
// numbers are also always rendered in Latin digits (no Arabic-numeral
// formatting prop), unlike upstream's `formatNumber` helper.
<Pagination count=100 pageSize=10 page=2 dir="rtl"/>
