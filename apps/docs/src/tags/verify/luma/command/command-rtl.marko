// GENERATED by build-verify-matrix.ts — do not edit
import Command from "@marko-ui/shadcn/ui/command/command.marko";

// Upstream drives this demo through a `useTranslation` hook (language
// selector component) that can switch between English/Arabic/Hebrew at
// runtime. We have no equivalent docs-site language-selector primitive to
// port, so this demo hardcodes the Arabic strings upstream defaults to
// (`useTranslation(translations, "ar")`) and passes `dir="rtl"` straight
// through — Command's native attrs (see command.marko's `nativeAttrs`)
// already forward `dir` to the root element and the input, so no
// component-source change was needed for this behavior.
//
// DEVIATION FROM UPSTREAM: upstream disables the "Calculator" item
// (`<CommandItem disabled>`). Our `CommandItem` type has no `disabled`
// field (see command-shortcuts.marko's comment for the same class of
// gap — items only carry `value`/`label`/`shortcut`), so every item here
// stays enabled. Icons (CalendarIcon, SmileIcon, CalculatorIcon, UserIcon,
// CreditCardIcon, SettingsIcon) are omitted for the same reason.
static const groups = [
  {
    label: "اقتراحات",
    items: [
      { value: "calendar", label: "التقويم" },
      { value: "search-emoji", label: "البحث عن الرموز التعبيرية" },
      { value: "calculator", label: "الآلة الحاسبة" },
    ],
  },
  {
    label: "الإعدادات",
    items: [
      { value: "profile", label: "الملف الشخصي", shortcut: "⌘P" },
      { value: "billing", label: "الفوترة", shortcut: "⌘B" },
      { value: "settings", label: "الإعدادات", shortcut: "⌘S" },
    ],
  },
];

<div class="w-full max-w-[450px]">
  <Command
    groups=groups
    placeholder="اكتب أمرًا أو ابحث..."
    dir="rtl"
    class="rounded-lg border shadow-md"
  />
</div>
