// GENERATED by build-verify-matrix.ts — do not edit
import Field from "@marko-ui/shadcn/ui/field/field.marko";
import FieldContent from "@marko-ui/shadcn/ui/field/field-content.marko";
import FieldDescription from "@marko-ui/shadcn/ui/field/field-description.marko";
import FieldGroup from "@marko-ui/shadcn/ui/field/field-group.marko";
import FieldLabel from "@marko-ui/shadcn/ui/field/field-label.marko";
import FieldSet from "@marko-ui/shadcn/ui/field/field-set.marko";
import FieldTitle from "@marko-ui/shadcn/ui/field/field-title.marko";
import RadioGroup from "@marko-ui/shadcn/ui/radio-group/radio-group.marko";

// Deviation from upstream: upstream wraps a bare <RadioGroupItem> in an
// external <FieldLabel><Field orientation="horizontal"><FieldContent>...
// with the indicator trailing the title/description text. Our
// <RadioGroup> renders every item as ONE root <label> (indicator, then
// hidden input, then item text, in that fixed order — see
// packages/shadcn/ui/radio-group/radio-group.marko) via the `item`
// attr-tag's `content` body, so a second wrapping <FieldLabel> would
// nest labels and the indicator cannot trail the text. We compose the
// "choice card" look through the item's own `content` slot, which puts
// the indicator first — a real, disclosed layout difference from
// upstream's trailing-indicator arrangement.
<div class="w-full max-w-md">
  <FieldGroup>
    <FieldSet>
      <FieldLabel>Compute Environment</FieldLabel>
      <FieldDescription>Select the compute environment for your cluster.</FieldDescription>
      <RadioGroup defaultValue="kubernetes" class="gap-3">
        <@item value="kubernetes">
          <FieldContent>
            <FieldTitle>Kubernetes</FieldTitle>
            <FieldDescription>Run GPU workloads on a K8s configured cluster.</FieldDescription>
          </FieldContent>
        </@item>
        <@item value="vm">
          <FieldContent>
            <FieldTitle>Virtual Machine</FieldTitle>
            <FieldDescription>Access a VM configured cluster to run GPU workloads.</FieldDescription>
          </FieldContent>
        </@item>
      </RadioGroup>
    </FieldSet>
  </FieldGroup>
</div>
