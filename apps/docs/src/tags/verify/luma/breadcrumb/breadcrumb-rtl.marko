// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: upstream drives this demo through a client-side
// language-selector (useTranslation) that has no equivalent in this docs
// app (see skills/port-to-marko — no such utility exists here; established
// precedent: demos/select/select-rtl.marko). Instead of faking an
// interactive language switcher, this ports the RTL VISUAL and markup
// behavior directly: a `dir="rtl"` breadcrumb with Arabic copy, matching
// upstream's default locale ("ar").
//
// DEVIATION FROM UPSTREAM: upstream separates items with lucide's DotIcon,
// which isn't in our icon set (packages/shadcn/ui/icon/icon-names.ts) —
// substituting the closest available icon, "Circle" (same substitution as
// breadcrumb-dropdown.marko).
import Breadcrumb from "@marko-ui/shadcn/ui/breadcrumb/breadcrumb.marko";
import BreadcrumbItem from "@marko-ui/shadcn/ui/breadcrumb/item.marko";
import BreadcrumbLink from "@marko-ui/shadcn/ui/breadcrumb/link.marko";
import BreadcrumbPage from "@marko-ui/shadcn/ui/breadcrumb/page.marko";
import BreadcrumbSeparator from "@marko-ui/shadcn/ui/breadcrumb/separator.marko";
import DropdownMenu from "@marko-ui/shadcn/ui/dropdown-menu/dropdown-menu.marko";
import Icon from "@marko-ui/shadcn/ui/icon/icon.marko";

static const ITEMS = [
  { value: "documentation", label: "التوثيق" },
  { value: "themes", label: "السمات" },
  { value: "github", label: "جيت هاب" },
];

<Breadcrumb dir="rtl">
  <BreadcrumbItem>
    <BreadcrumbLink href="/">الرئيسية</BreadcrumbLink>
  </BreadcrumbItem>
  <BreadcrumbSeparator>
    <Icon name="Circle" class="size-3.5 fill-current"/>
  </BreadcrumbSeparator>
  <BreadcrumbItem>
    <DropdownMenu items=ITEMS>
      <@trigger|triggerProps|>
        <button ...triggerProps class="flex items-center gap-1" dir="rtl">
          المكونات
          <Icon name="ChevronDown" class="size-3.5"/>
        </button>
      </@trigger>
    </DropdownMenu>
  </BreadcrumbItem>
  <BreadcrumbSeparator>
    <Icon name="Circle" class="size-3.5 fill-current"/>
  </BreadcrumbSeparator>
  <BreadcrumbItem>
    <BreadcrumbPage>مسار التنقل</BreadcrumbPage>
  </BreadcrumbItem>
</Breadcrumb>
