// GENERATED by build-verify-matrix.ts — do not edit
import HoverCard from "@marko-ui/shadcn/ui/hover-card/hover-card.marko";
import Button from "@marko-ui/shadcn/ui/button/button.marko";

// Upstream's hover-card-rtl demo drives a live language switcher
// (useTranslation) with no equivalent in this docs app — see
// demos/tooltip/tooltip-rtl.marko and demos/popover/popover-rtl.marko for
// the established precedent. This ports the RTL VISUAL and behavior
// directly: a dir="rtl" row with Arabic copy, matching the source's
// default locale ("ar"). Upstream's `side` prop (left/top/bottom/right)
// maps directly onto Zag's positioning.placement — floating-ui
// auto-flips left/right placements under dir="rtl", the actual behavior
// the upstream demo showcases.
//
// Deliberate omission: upstream's hover-card-rtl.tsx (.upstream/
// shadcn-ui/apps/v4/examples/base/hover-card-rtl.tsx) renders TWO rows —
// a `physicalSides` row (left/top/bottom/right, below) and a second
// `logicalSides` row (inline-start/inline-end). We only port the
// physical-sides row, matching tooltip-rtl.marko's precedent: Zag's
// positioning.placement has no logical-side values to feed a second row
// with — there's no `dir`-relative "inline-start"/"inline-end" placement
// in floating-ui's Placement type, so there's nothing faithful to port
// that row to.
<div dir="rtl" class="flex flex-wrap justify-center gap-2">
  <HoverCard dir="rtl" openDelay=10 closeDelay=100 positioning={ placement: "left" }>
    <@trigger|props|>
      <Button ...props variant="outline">يسار</Button>
    </@trigger>
    <@content>
      <div dir="rtl" class="flex flex-col gap-1">
        <div class="font-semibold">سماعات لاسلكية</div>
        <div class="text-sm text-muted-foreground">٩٩.٩٩ $</div>
      </div>
    </@content>
  </HoverCard>
  <HoverCard dir="rtl" openDelay=10 closeDelay=100 positioning={ placement: "top" }>
    <@trigger|props|>
      <Button ...props variant="outline">أعلى</Button>
    </@trigger>
    <@content>
      <div dir="rtl" class="flex flex-col gap-1">
        <div class="font-semibold">سماعات لاسلكية</div>
        <div class="text-sm text-muted-foreground">٩٩.٩٩ $</div>
      </div>
    </@content>
  </HoverCard>
  <HoverCard dir="rtl" openDelay=10 closeDelay=100 positioning={ placement: "bottom" }>
    <@trigger|props|>
      <Button ...props variant="outline">أسفل</Button>
    </@trigger>
    <@content>
      <div dir="rtl" class="flex flex-col gap-1">
        <div class="font-semibold">سماعات لاسلكية</div>
        <div class="text-sm text-muted-foreground">٩٩.٩٩ $</div>
      </div>
    </@content>
  </HoverCard>
  <HoverCard dir="rtl" openDelay=10 closeDelay=100 positioning={ placement: "right" }>
    <@trigger|props|>
      <Button ...props variant="outline">يمين</Button>
    </@trigger>
    <@content>
      <div dir="rtl" class="flex flex-col gap-1">
        <div class="font-semibold">سماعات لاسلكية</div>
        <div class="text-sm text-muted-foreground">٩٩.٩٩ $</div>
      </div>
    </@content>
  </HoverCard>
</div>
