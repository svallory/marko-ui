// GENERATED by build-verify-matrix.ts — do not edit
// Static RTL port: upstream's tabs-rtl demo drives a live docs-site
// language switcher (useTranslation across en/ar/he) that has no
// equivalent in this app — same established precedent as
// demos/card/card-rtl.marko, demos/accordion/accordion-rtl.marko,
// demos/input/input-rtl.marko, and demos/tooltip/tooltip-rtl.marko. This
// ports the RTL VISUAL and markup directly using the source's default
// locale ("ar"): a `dir="rtl"` tabs list with Arabic copy, each panel
// containing a Card matching upstream's tabs-rtl composition. Tabs'
// layout is Tailwind flex with no hard-coded physical margins on
// RTL-sensitive elements, so it restyles correctly under dir="rtl" with
// no component changes needed.
import Tabs from "@marko-ui/shadcn/ui/tabs/tabs.marko";
import Card from "@marko-ui/shadcn/ui/card/card.marko";
import CardHeader from "@marko-ui/shadcn/ui/card/header.marko";
import CardTitle from "@marko-ui/shadcn/ui/card/title.marko";
import CardDescription from "@marko-ui/shadcn/ui/card/description.marko";
import CardContent from "@marko-ui/shadcn/ui/card/content.marko";

<Tabs class="w-full max-w-sm" dir="rtl">
  <@trigger value="overview">نظرة عامة</@trigger>
  <@trigger value="analytics">التحليلات</@trigger>
  <@trigger value="reports">التقارير</@trigger>
  <@trigger value="settings">الإعدادات</@trigger>
  <@panel value="overview">
    <Card dir="rtl">
      <CardHeader>
        <CardTitle>نظرة عامة</CardTitle>
        <CardDescription>
          عرض مقاييسك الرئيسية وأنشطة المشروع الأخيرة. تتبع التقدم عبر جميع مشاريعك النشطة.
        </CardDescription>
      </CardHeader>
      <CardContent class="text-muted-foreground text-sm">
        لديك ١٢ مشروعًا نشطًا و٣ مهام معلقة.
      </CardContent>
    </Card>
  </@panel>
  <@panel value="analytics">
    <Card dir="rtl">
      <CardHeader>
        <CardTitle>التحليلات</CardTitle>
        <CardDescription>
          تتبع مقاييس الأداء ومشاركة المستخدمين. راقب الاتجاهات وحدد فرص النمو.
        </CardDescription>
      </CardHeader>
      <CardContent class="text-muted-foreground text-sm">
        زادت مشاهدات الصفحة بنسبة ٢٥٪ مقارنة بالشهر الماضي.
      </CardContent>
    </Card>
  </@panel>
  <@panel value="reports">
    <Card dir="rtl">
      <CardHeader>
        <CardTitle>التقارير</CardTitle>
        <CardDescription>
          إنشاء وتنزيل تقاريرك التفصيلية. تصدير البيانات بتنسيقات متعددة للتحليل.
        </CardDescription>
      </CardHeader>
      <CardContent class="text-muted-foreground text-sm">
        لديك ٥ تقارير جاهزة ومتاحة للتصدير.
      </CardContent>
    </Card>
  </@panel>
  <@panel value="settings">
    <Card dir="rtl">
      <CardHeader>
        <CardTitle>الإعدادات</CardTitle>
        <CardDescription>
          إدارة تفضيلات حسابك وخياراته. تخصيص تجربتك لتناسب احتياجاتك.
        </CardDescription>
      </CardHeader>
      <CardContent class="text-muted-foreground text-sm">
        تكوين الإشعارات والأمان والسمات.
      </CardContent>
    </Card>
  </@panel>
</Tabs>
