// GENERATED by build-verify-matrix.ts — do not edit
import Pagination from "@marko-ui/shadcn/ui/pagination/pagination.marko";
import Field from "@marko-ui/shadcn/ui/field/field.marko";
import FieldLabel from "@marko-ui/shadcn/ui/field/field-label.marko";
import Select from "@marko-ui/shadcn/ui/select/select.marko";

static const rowsPerPage = [
  { value: "10", label: "10" },
  { value: "25", label: "25" },
  { value: "50", label: "50" },
  { value: "100", label: "100" },
];

// Upstream's "Icons Only" demo shows just the prev/next buttons (useful next
// to a rows-per-page selector). Our Pagination is a flat, machine-driven
// component (see docs.ts note) — it always renders its page-number list, so
// there is no prop to hide it. This ports the VISUAL/layout faithfully
// (Field + Select rows-per-page control beside a nav) but the pagination
// half necessarily still shows page numbers; a true icons-only mode would
// need a new prop on the component (out of demo territory).
<div class="flex items-center justify-between gap-4">
  <Field orientation="horizontal" class="w-fit">
    <FieldLabel for="pagination-icons-only-rows">Rows per page</FieldLabel>
    <Select id="pagination-icons-only-rows" items=rowsPerPage defaultValue=["25"] class="w-20"/>
  </Field>
  <Pagination count=100 pageSize=10 page=1 class="mx-0 w-auto"/>
</div>
