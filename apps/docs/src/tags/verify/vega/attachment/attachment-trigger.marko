// GENERATED by build-verify-matrix.ts — do not edit
import Attachment from "@marko-ui/shadcn/ui/attachment/attachment.marko";
import AttachmentMedia from "@marko-ui/shadcn/ui/attachment/media.marko";
import AttachmentContent from "@marko-ui/shadcn/ui/attachment/content.marko";
import AttachmentTitle from "@marko-ui/shadcn/ui/attachment/title.marko";
import AttachmentDescription from "@marko-ui/shadcn/ui/attachment/description.marko";
import AttachmentActions from "@marko-ui/shadcn/ui/attachment/actions.marko";
import AttachmentAction from "@marko-ui/shadcn/ui/attachment/action.marko";
import AttachmentTrigger from "@marko-ui/shadcn/ui/attachment/trigger.marko";
import Dialog from "@marko-ui/shadcn/ui/dialog/dialog.marko";
import Icon from "@marko-ui/shadcn/ui/icon/icon.marko";

// Upstream wraps <Attachment> in <Dialog> and swaps AttachmentTrigger's
// props onto Radix's <DialogTrigger render={...}>. Our Dialog is a single
// compound tag (see /docs/components/dialog) whose `<@trigger|props|>` attr
// tag hands back `api().getTriggerProps()` to spread onto the caller's own
// element — so the render-prop merge becomes spreading those props onto our
// AttachmentTrigger directly.
<Dialog>
  <@trigger|props|>
    <Attachment class="w-full">
      <AttachmentMedia>
        <Icon name="FileSearchIcon"/>
      </AttachmentMedia>
      <AttachmentContent>
        <AttachmentTitle>research-summary.pdf</AttachmentTitle>
        <AttachmentDescription>Open preview dialog</AttachmentDescription>
      </AttachmentContent>
      <AttachmentActions>
        <AttachmentAction aria-label="Copy link">
          <Icon name="Copy"/>
        </AttachmentAction>
        <AttachmentAction aria-label="Remove research-summary.pdf">
          <Icon name="X"/>
        </AttachmentAction>
      </AttachmentActions>
      <AttachmentTrigger ...props aria-label="Preview research-summary.pdf"/>
    </Attachment>
  </@trigger>
  <@title>research-summary.pdf</@title>
  <@description>
    The attachment trigger fills the card and opens the dialog, while the
    actions stay independently clickable above it.
  </@description>
</Dialog>
