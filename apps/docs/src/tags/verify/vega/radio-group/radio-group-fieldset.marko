// GENERATED by build-verify-matrix.ts — do not edit
// Upstream wraps each RadioGroupItem in its own <Field orientation="horizontal">
// because Base UI's RadioGroupItem is a standalone primitive. Our RadioGroup
// renders every item's label+control pair internally (see
// packages/shadcn/ui/radio-group/radio-group.marko — there is no standalone
// RadioGroupItem tag to wrap), so the outer FieldSet/FieldLegend/
// FieldDescription composition ports faithfully but the per-item <Field>
// wrapper is dropped; the built-in item markup already provides equivalent
// label+control spacing.
import RadioGroup from "@marko-ui/shadcn/ui/radio-group/radio-group.marko";
import FieldSet from "@marko-ui/shadcn/ui/field/field-set.marko";
import FieldLegend from "@marko-ui/shadcn/ui/field/field-legend.marko";
import FieldDescription from "@marko-ui/shadcn/ui/field/field-description.marko";

static const PLANS = [
  { value: "monthly", label: "Monthly ($9.99/month)" },
  { value: "yearly", label: "Yearly ($99.99/year)" },
  { value: "lifetime", label: "Lifetime ($299.99)" },
];

<FieldSet class="w-full max-w-xs">
  <FieldLegend variant="label">Subscription Plan</FieldLegend>
  <FieldDescription>Yearly and lifetime plans offer significant savings.</FieldDescription>
  <RadioGroup items=PLANS value="monthly" class="mt-3"/>
</FieldSet>
