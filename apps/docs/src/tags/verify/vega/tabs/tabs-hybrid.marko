// GENERATED by build-verify-matrix.ts — do not edit
import Tabs from "@marko-ui/registry/styles/vega/ui/tabs/tabs.marko";

// Exercises the documented HYBRID gotcha: `trigger` and `panel` normalize
// independently, so <@trigger> tags mixed with `items=` (and no <@panel>)
// yield attr-tag-derived triggers paired with items=-derived panels.
<Tabs|value| items=[{ value: "one", label: "Items One" }, { value: "two", label: "Items Two" }]>
  <@trigger value="one">Tag One</@trigger>
  <@trigger value="two">Tag Two</@trigger>
  Panel for ${value}
</Tabs>
