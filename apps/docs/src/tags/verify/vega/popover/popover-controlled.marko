// GENERATED by build-verify-matrix.ts — do not edit
import Popover from "@marko-ui/registry/styles/vega/ui/popover/popover.marko";
import Button from "@marko-ui/registry/styles/vega/ui/button/button.marko";

<let/open=false/>

<div class="flex items-center gap-4">
  <Popover
    open=open
    openChange=(next) => { open = next; }
  >
    <@trigger|props|>
      <Button ...props variant="outline">${open ? "Close" : "Open"}</Button>
    </@trigger>
    <@content>
      <p class="text-sm">Controlled popover content.</p>
    </@content>
  </Popover>
  <span class="text-sm text-muted-foreground">open: ${String(open)}</span>
</div>
