// GENERATED by build-verify-matrix.ts — do not edit
import Command from "@marko-ui/registry/styles/rhea/ui/command/command.marko";

static const groups = [
  {
    label: "Fruits",
    items: [
      { value: "apple", label: "Apple" },
      { value: "banana", label: "Banana" },
      { value: "orange", label: "Orange" },
    ],
  },
];

// Search for something that matches no item, e.g. "xyz", to see the
// "No results found." state the component falls back to.
<div class="w-full max-w-[450px]">
  <Command groups=groups placeholder="Try searching for “xyz”..." class="rounded-lg border shadow-md"/>
</div>
