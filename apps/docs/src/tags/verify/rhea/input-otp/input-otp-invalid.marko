// GENERATED by build-verify-matrix.ts — do not edit
import InputOTP from "@marko-ui/shadcn/ui/input-otp/input-otp.marko";

// Upstream sets `aria-invalid` per-slot on the React <InputOTPSlot>. Our
// component is a single tag backed by one Zag pin-input machine, whose
// `invalid` prop applies `aria-invalid`/`data-invalid` uniformly to every
// slot (there is no per-slot invalid API) — functionally equivalent for
// this all-invalid demo, so `invalid` replaces the per-slot prop here.
<InputOTP length=6 defaultValue=["0","0","0","0","0","0"] groupSize=2 invalid/>
