// GENERATED by build-verify-matrix.ts — do not edit
import Tooltip from "@marko-ui/shadcn/ui/tooltip/tooltip.marko";
import Button from "@marko-ui/shadcn/ui/button/button.marko";

// Upstream's tooltip-rtl demo drives a live language switcher (docs-site
// chrome we haven't ported — see notes/docs-canonical-structure.md, RTL
// section is component behavior, not site machinery) and passes Base UI's
// `side` prop literal values "inline-start"/"inline-end", which Base UI's
// own popper resolves against `dir`. Our Tooltip is Zag-backed; Zag's
// positioning.placement is floating-ui's physical Placement type (top /
// bottom / left / right, no logical values) — see
// @zag-js/popper's placement.d.ts. The faithful equivalent is Zag's own
// `dir` machine prop: floating-ui auto-flips left/right placements under
// dir="rtl", which is the actual behavior the upstream demo showcases.
//
// Deliberate omission: upstream's tooltip-rtl.tsx (.upstream/shadcn-ui/
// apps/v4/examples/base/tooltip-rtl.tsx) renders TWO rows — a
// `physicalSides` row (left/top/bottom/right, below) and a second
// `logicalSides` row (inline-start/inline-end). We only port the
// physical-sides row. Zag's positioning.placement has no logical-side
// values to feed a second row with — there's no `dir`-relative
// "inline-start"/"inline-end" placement in floating-ui's Placement type,
// so there's nothing faithful to port that row to.
<div dir="rtl" class="flex flex-wrap justify-center gap-2">
  <Tooltip content="إضافة إلى المكتبة" dir="rtl" positioning={ placement: "left" }>
    <@trigger|props|>
      <Button ...props variant="outline">يسار</Button>
    </@trigger>
  </Tooltip>
  <Tooltip content="إضافة إلى المكتبة" dir="rtl" positioning={ placement: "top" }>
    <@trigger|props|>
      <Button ...props variant="outline">أعلى</Button>
    </@trigger>
  </Tooltip>
  <Tooltip content="إضافة إلى المكتبة" dir="rtl" positioning={ placement: "bottom" }>
    <@trigger|props|>
      <Button ...props variant="outline">أسفل</Button>
    </@trigger>
  </Tooltip>
  <Tooltip content="إضافة إلى المكتبة" dir="rtl" positioning={ placement: "right" }>
    <@trigger|props|>
      <Button ...props variant="outline">يمين</Button>
    </@trigger>
  </Tooltip>
</div>
