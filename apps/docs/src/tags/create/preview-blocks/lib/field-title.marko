// Stand-in for shadcn's ui/field.tsx FieldTitle — see field-group.marko's
// header. Nova defaults baked in.
import { cn } from "@marko-ui/registry/lib/utils.ts";

export interface Input extends Marko.Input<"div"> {}

<const/{ class: className, content, ...rest }=input/>

<div
  ...rest
  data-slot="field-label"
  class=cn(
    "cn-field-title flex w-fit items-center gap-2 leading-snug text-sm font-medium group-data-[disabled=true]/field:opacity-50",
    className,
  )
>
  <${content}/>
</div>
