// Stand-in for shadcn's ui/field.tsx FieldContent — see field-group.marko's
// header. Nova defaults baked in.
import { cn } from "@marko-ui/registry/lib/utils.ts";

export interface Input extends Marko.Input<"div"> {}

<const/{ class: className, content, ...rest }=input/>

<div
  ...rest
  data-slot="field-content"
  class=cn(
    "cn-field-content group/field-content flex flex-1 flex-col gap-0.5 leading-snug",
    className,
  )
>
  <${content}/>
</div>
