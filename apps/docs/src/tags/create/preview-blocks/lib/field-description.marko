// Stand-in for shadcn's ui/field.tsx FieldDescription — see
// field-group.marko's header. Nova defaults baked in.
import { cn } from "@marko-ui/registry/lib/utils.ts";

export interface Input extends Marko.Input<"p"> {}

<const/{ class: className, content, ...rest }=input/>

<p
  ...rest
  data-slot="field-description"
  class=cn(
    "cn-field-description text-muted-foreground text-left text-sm leading-normal font-normal",
    "last:mt-0 nth-last-2:-mt-1",
    "[&>a]:underline [&>a]:underline-offset-4 [&>a:hover]:text-primary",
    className,
  )
>
  <${content}/>
</p>
