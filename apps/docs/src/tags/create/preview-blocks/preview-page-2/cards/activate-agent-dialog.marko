// Ported from shadcn registry/bases/base/blocks/preview/cards/activate-agent-dialog.tsx
import Alert from "@marko-ui/registry/ui/alert/alert.marko";
import AlertDescription from "@marko-ui/registry/ui/alert/description.marko";
import Badge from "@marko-ui/registry/ui/badge/badge.marko";
import Button from "@marko-ui/registry/ui/button/button.marko";
import Card from "@marko-ui/registry/ui/card/card.marko";
import CardContent from "@marko-ui/registry/ui/card/content.marko";
import CardDescription from "@marko-ui/registry/ui/card/description.marko";
import CardFooter from "@marko-ui/registry/ui/card/footer.marko";
import CardHeader from "@marko-ui/registry/ui/card/header.marko";
import CardTitle from "@marko-ui/registry/ui/card/title.marko";
import Item from "@marko-ui/registry/ui/item/item.marko";
import ItemContent from "@marko-ui/registry/ui/item/content.marko";
import ItemGroup from "@marko-ui/registry/ui/item/group.marko";
import ItemMedia from "@marko-ui/registry/ui/item/media.marko";
import ItemTitle from "@marko-ui/registry/ui/item/title.marko";
import Icon from "../../lib/icon.marko";

<Card>
  <CardHeader>
    <CardTitle>Ship faster & safer with Vercel Agent</CardTitle>
    <CardDescription>
      Your use is subject to Vercel's <a href="#">Public Beta Agreement</a>
      and <a href="#">AI Product Terms</a>.
    </CardDescription>
  </CardHeader>
  <CardContent class="flex flex-col gap-4">
    <ItemGroup class="gap-0">
      <Item size="sm" class="px-0">
        <ItemMedia variant="icon" class="self-start">
          <Icon name="CheckCircle2Icon" class="size-5 fill-primary text-primary-foreground"/>
        </ItemMedia>
        <ItemContent>
          <ItemTitle class="inline leading-relaxed font-normal text-muted-foreground *:[strong]:font-medium *:[strong]:text-foreground">
            <strong>Code reviews</strong> with full codebase context to catch
            <strong>hard-to-find</strong> bugs.
          </ItemTitle>
        </ItemContent>
      </Item>
      <Item size="sm" class="px-0">
        <ItemMedia variant="icon" class="self-start">
          <Icon name="CheckCircle2Icon" class="size-5 fill-primary text-primary-foreground"/>
        </ItemMedia>
        <ItemContent>
          <ItemTitle class="inline leading-relaxed font-normal text-muted-foreground *:[strong]:font-medium *:[strong]:text-foreground">
            <strong>Code suggestions</strong> validated in sandboxes before you
            merge.
          </ItemTitle>
        </ItemContent>
      </Item>
      <Item size="sm" class="px-0">
        <ItemMedia variant="icon" class="self-start">
          <Icon name="CheckCircle2Icon" class="size-5 fill-primary text-primary-foreground"/>
        </ItemMedia>
        <ItemContent>
          <ItemTitle class="inline leading-relaxed font-normal text-muted-foreground *:[strong]:font-medium *:[strong]:text-foreground">
            <strong>Root-cause analysis</strong> for production issues with
            deployment context.
            <Badge variant="secondary" class="bg-chart-1 text-chart-5">
              Requires Observability Plus
            </Badge>
          </ItemTitle>
        </ItemContent>
      </Item>
    </ItemGroup>
    <Alert>
      <AlertDescription>
        Pro teams get $100 in Vercel Agent trial credit for 2 weeks after
        activation.
      </AlertDescription>
    </Alert>
  </CardContent>
  <CardFooter class="justify-end gap-2">
    <Button variant="outline" class="style-sera:hidden">
      Cancel
    </Button>
    <Button class="style-sera:w-full">Enable with $100 credits</Button>
  </CardFooter>
</Card>
