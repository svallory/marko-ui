// Changelog — structural counterpart to shadcn's content/docs/changelog/*.mdx
// (dated entries, newest first). Entries are drawn from merged PR titles on
// main (`gh pr list --state merged`) and the release commits that bumped
// packages/marko-ui and packages/shadcn's package.json versions. No git tags
// exist in this repo; version history is tracked only via those release
// commits, so dates below are the release commit's merge date.
import "../../../tags/docs/docs-article.marko";
import { COMPONENTS } from "../../../lib/components-list.ts";

static const TOC = [
  { id: "unreleased", label: "Unreleased" },
  { id: "0.3.1", label: "0.3.1" },
  { id: "0.3.0", label: "0.3.0" },
  { id: "0.2.1", label: "0.2.1" },
  { id: "0.2.0", label: "0.2.0" },
  { id: "versioning", label: "Versioning" },
];

export interface ChangelogEntry {
  version: string;
  date: string;
  title: string;
  summary: string;
  changes: string[];
}

static const ENTRIES: ChangelogEntry[] = [
  {
    version: "0.3.1",
    date: "2026-09-17",
    title: "Honest numbers, a green pipeline, and the no-JS form claim proved",
    summary:
      "A correctness pass over everything the project claims publicly: each published number was re-derived from the repo and corrected where it had drifted, the CI pipeline was made genuinely green, and the headline \"forms validate without JavaScript\" claim became a test instead of an assertion.",
    changes: [
      "Fixed `marko-ui init` crashing on a freshly scaffolded Marko app: with no Tailwind stylesheet to detect yet, it fell back to shadcn's inherited Next.js `app/globals.css`, wrote that into components.json, and then died with ENOENT on the very path it had just failed to find. Marko projects now fall back to `src/styles/globals.css`.",
      "`--visual-style`'s help text no longer claims it is \"ignored for copy\" — the copy path fetches per-style registry items, so the style selects which generated source is copied in.",
      "The documented install commands now pass `--base-color`, so the \"non-interactive\" form really is non-interactive.",
      "Fixed the `Publish badges` CI job, which had failed on every push to main since the style-matrix job was removed — it still required that job's artifact, so the run showed red while all four substantive jobs passed.",
      "Raised syntax-highlighting contrast to WCAG AA. Three shiki token colors sat just under 4.5:1 on the code background, which was the sole cause of the Lighthouse accessibility score sitting at 96/97; it is 100 on all audited pages now.",
      "Added a no-JS form route and a browserless test that POSTs to it and asserts the valibot messages are present in the returned HTML — the \"forms validate without JavaScript\" claim had no in-repo verification before this.",
      "Corrected the public numbers: 8 visual styles (not 9 — \"default\" was a vestigial shadcn field), hydration coverage stated as 33 of 54 rather than implying all, and the real keyboard-contract test count (71) in place of an unreproducible one.",
      "Documented a phantom docs type-check failure that is environmental, not a repo defect: with the AI_AGENT variable set and `bun` resolving through a proto shim, an NDJSON banner lands in the type checker's captured output and is fingerprinted as an error that exists nowhere in the source. Run `env -u AI_AGENT bun run check`; CI is never affected.",
      "0.3.1 onward is published through the OIDC trusted-publishing workflow and carries npm provenance attestations; 0.3.0 and earlier do not.",
      "Breaking: adopted marko-zag 3.0.0's automatic `Props` shadowing — a component's declared member now always wins over a same-named native attribute, with no `Omit` or 3rd type-parameter gymnastics. Every component using the retired patterns was migrated.",
      "The docs app's type-check baseline was cleared to 0 known errors and the gate scripts hardened with their own fixture tests.",
      "Fixed the docs site's axe-core WCAG scan to 0 violations (keyboard-unreachable scroll containers, page heading order).",
    ],
  },
  {
    version: "0.3.0",
    date: "2026-09-13",
    title: "Class-as-data styling, Input→TextInput rename",
    summary:
      "The per-style class-generation pipeline moved from a text transform to typed data, and a naming collision between a component and Marko's reserved props-type name was fixed across every consumer.",
    changes: [
      "Breaking: every docs-app import of the text-input component's `Input` binding renamed to `TextInput` — importing it as `Input` shadowed Marko's reserved props-type name and collapsed prop getters to `any` in that file.",
      "New class-as-data pipeline: each component's `classes.ts` (pure data, one export per part) is resolved against a style's CSS and merged into the copy-path file at build time, replacing the previous text-transform step. Piloted on button/accordion/sidebar, then rolled out and the old text transform retired.",
      "Attr-tag typing fixes: parameterized and parameterless attr-tag fields declared as `Marko.AttrTag<{ content }>` instead of bare `Marko.Body`, fixing consumer-side TS2353 errors on 17 fields across alert-dialog/dialog/drawer/sheet/avatar/floating-panel/qr-code/color-picker/steps and two docs-app tags.",
      "Native-attribute shadowing fixed on title-declaring components (alert-dialog, dialog, drawer, sheet) — a declared `title` member was being intersected with, not overriding, the native `title` attribute, producing an unsatisfiable type.",
      "marko-zag caret range fix: pinned to `^2.0.1` instead of an exact version, so a consumer app with its own marko-zag dependency can dedupe instead of getting a second nested copy.",
      "The old style-matrix e2e suite (`e2e/verify-matrix.spec.ts`, the generated verify route tree, and the `build:verify` CI job) was removed — its coverage was superseded by behavior tests, the hydration-invariant suite, and the docs pages themselves, and it duplicated the same bugs 9x across generated style copies.",
      "A baseline-diff gate was added for the docs app's `@marko/type-check` run, replacing a raw error-count threshold that had proven unreliable across dependency bumps.",
    ],
  },
  {
    version: "0.2.1",
    date: "2026-09-08",
    title: "CLI symlink fix, acceptance suite",
    summary: "A real CLI bug fix, plus a new test suite that runs against the published npm packages instead of the workspace.",
    changes: [
      "Fixed a CLI entrypoint bug: on macOS, invoking the CLI through a path containing a symlinked directory (e.g. `/var` → `/private/var`, which `os.tmpdir()` returns) made the entrypoint guard's comparison fail silently — the process exited 0 having done nothing, with no output and no error.",
      "Added `e2e/acceptance/`: a suite that scaffolds real apps against the published `marko-ui`/`@marko-ui/shadcn` npm packages and the live registry deploy, covering the copy path, the import path, `doctor`/`diff`, and registry health.",
      "TypeScript pinned to `^6.0` (downgraded from an attempted 7.x) — TS 7's `tsgo` ships no in-process compiler API, which both the docs type-check tooling and the marko-ui CLI's build depend on.",
      "Calendar demos pinned to a fixed `defaultFocusedValue` instead of relying on the browser's real \"today\", stopping day-of-month snapshot drift.",
    ],
  },
  {
    version: "0.2.0",
    date: "2026-09-03",
    title: "First tagged release",
    summary: "marko-ui and @marko-ui/shadcn published to npm for the first time, alongside the attr-tag compound API, full CSS architecture, and the migration to marko-zag's single `<zag>` tag.",
    changes: [
      "Breaking: migrated every Zag-backed component from the three-tag `<machine-props>`/`<service>`/`<connect>` wiring to marko-zag 2.0's single `<zag/api=() => mod from=input/>` tag.",
      "Attribute-tag compound API shipped across the multi-part components that need it (dropdown-menu, menubar, context-menu submenus, and others).",
      "Full shadcn CSS architecture ported, including all 8 style layers (rhea, nova, vega, lyra, maia, mira, luma, sera) as vendored token CSS.",
      "Registry Directory and the port-to-marko contribution workflow shipped, giving external contributors a documented path to add a component.",
      "marko bumped to 6.3.46; the `connectFresh` walk-order workarounds it had required were removed.",
      "Site URL centralized into a `VITE_SITE_URL` env var read through `import.meta.env`, replacing a hardcoded domain.",
    ],
  },
];

<docs-article
  title="Changelog"
  description="Notable changes to marko-ui components, the registry, and the marko-ui adapter."
  href="/docs/changelog"
  toc=TOC
>
  <h2 id="unreleased">Unreleased</h2>

  <p>
    Nothing yet: everything merged to <code>main</code> since <code>0.3.0</code> is in the
    <a href="#0.3.1"><code>0.3.1</code></a> entry below.
  </p>

  <p>Currently in the registry: ${COMPONENTS.length} components, plus the theme and utils items.</p>

  <for|entry| of=ENTRIES by=(entry)=>entry.version>
    <h2 id=entry.version>${entry.version}</h2>
    <div class="mt-8 border-l-2 border-border pl-5">
      <div class="text-xs font-medium tracking-wide text-muted-foreground uppercase">
        ${entry.date}
      </div>
      <h3 class="mt-1 text-base font-semibold tracking-tight">${entry.title}</h3>
      <p>${entry.summary}</p>
      <ul>
        <for|change| of=entry.changes>
          <li>${change}</li>
        </for>
      </ul>
    </div>
  </for>

  <h2 id="versioning">Versioning</h2>

  <p>
    Because component source lives in your repo rather than in a package you upgrade, marko-ui has
    two independent version surfaces:
  </p>

  <ul>
    <li>
      <strong>The registry</strong> is unversioned and always reflects the current main branch.
      Components you already installed are unaffected by changes here — you take an update only by
      re-running <code>add --overwrite</code>.
    </li>
    <li>
      <strong><code>marko-ui</code></strong> is a normal npm package and follows semver. It is the
      adapter binding Zag machines into Marko, and it is the dependency that actually ships fixes
      to you.
    </li>
  </ul>

  <p>
    Before taking a component update, use <code>diff</code> to see exactly what would change.
    See <a href="/docs/cli">CLI</a>.
  </p>
</docs-article>
