import { cn } from "#lib/utils.ts";
import { input as styles } from "./classes.ts";

export interface Input extends Marko.Input<"input"> {}

<const/{ type, class: className, ...rest }=input/>

<input
  type=type
  data-slot="input"
  class=cn(styles.root, className)
  ...rest
/>
