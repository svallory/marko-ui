import { cn } from "#lib/utils.ts";
import { cell as styles } from "./classes.ts";

export interface Input extends Marko.Input<"td"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<td
  ...rest
  data-slot="table-cell"
  class=cn(styles.root, className)
>
  <${content}/>
</td>
