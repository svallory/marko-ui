import { cn } from "#lib/utils.ts";
import { caption as styles } from "./classes.ts";

export interface Input extends Marko.Input<"caption"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<caption ...rest data-slot="table-caption" class=cn(styles.root, className)>
  <${content}/>
</caption>
