import { cn } from "#lib/utils.ts";
import { header as styles } from "./classes.ts";

export interface Input extends Marko.Input<"thead"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<thead ...rest data-slot="table-header" class=cn(styles.root, className)>
  <${content}/>
</thead>
