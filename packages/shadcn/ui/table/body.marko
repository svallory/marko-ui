import { cn } from "#lib/utils.ts";
import { body as styles } from "./classes.ts";

export interface Input extends Marko.Input<"tbody"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<tbody ...rest data-slot="table-body" class=cn(styles.root, className)>
  <${content}/>
</tbody>
