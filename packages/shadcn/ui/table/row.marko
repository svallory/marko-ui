import { cn } from "#lib/utils.ts";
import { row as styles } from "./classes.ts";

export interface Input extends Marko.Input<"tr"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<tr
  ...rest
  data-slot="table-row"
  class=cn(styles.root, className)
>
  <${content}/>
</tr>
