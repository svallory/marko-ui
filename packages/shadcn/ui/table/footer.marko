import { cn } from "#lib/utils.ts";
import { footer as styles } from "./classes.ts";

export interface Input extends Marko.Input<"tfoot"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<tfoot
  ...rest
  data-slot="table-footer"
  class=cn(styles.root, className)
>
  <${content}/>
</tfoot>
