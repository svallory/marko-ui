import { cn } from "#lib/utils.ts";
import { table as styles } from "./classes.ts";

export interface Input extends Marko.Input<"div"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<div ...rest data-slot="table-container" class=cn(styles.container, className)>
  <table data-slot="table" class=styles.root>
    <${content}/>
  </table>
</div>
