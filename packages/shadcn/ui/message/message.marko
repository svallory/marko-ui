import { cn } from "#lib/utils.ts";
import { message as styles } from "./classes.ts";

export interface Input extends Marko.Input<"div"> {
  align?: "start" | "end";
}

<const/{ align = "start", class: className, content, ...rest }=input/>

<div
  data-slot="message"
  data-align=align
  class=cn(styles.root, className)
  ...rest
>
  <${content}/>
</div>
