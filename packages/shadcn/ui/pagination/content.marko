import { cn } from "#lib/utils.ts";
import { content as styles } from "./classes.ts";

export interface Input extends Marko.Input<"ul"> {}

<const/{ class: className, content, ...rest }=input/>

<ul data-slot="pagination-content" class=cn(styles.root, className) ...rest>
  <${content}/>
</ul>
