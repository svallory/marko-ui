import { cn } from "#lib/utils.ts";
import { next as styles } from "./classes.ts";
import PaginationLink from "./link.marko";

export interface Input extends Marko.Input<"a"> {
  text?: string;
}

<const/{ class: className, text = "Next", ...rest }=input/>

<PaginationLink
  aria-label="Go to next page"
  size="default"
  class=cn(styles.root, className)
  ...rest
>
  <span class=styles.text>${text}</span>
  <svg
    xmlns="http://www.w3.org/2000/svg"
    width="24"
    height="24"
    viewBox="0 0 24 24"
    fill="none"
    stroke="currentColor"
    stroke-width="2"
    stroke-linecap="round"
    stroke-linejoin="round"
    data-icon="inline-end"
    class=styles.icon
  >
    <path d="m9 18 6-6-6-6"/>
  </svg>
</PaginationLink>
