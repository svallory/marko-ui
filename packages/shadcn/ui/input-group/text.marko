import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"span"> {}

<const/{ class: className, content, ...rest }=input/>

<span
  ...rest
  class=cn(
    "mu-input-group-text flex items-center [&_svg]:pointer-events-none",
    className,
  )
>
  <${content}/>
</span>
