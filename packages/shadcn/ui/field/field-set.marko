import { cn } from "#lib/utils.ts";
import { fieldSet as styles } from "./classes.ts";

export interface Input extends Marko.Input<"fieldset"> {}

<const/{ class: className, content, ...rest }=input/>

<fieldset
  data-slot="field-set"
  class=cn(styles.root, className)
  ...rest
>
  <${content}/>
</fieldset>
