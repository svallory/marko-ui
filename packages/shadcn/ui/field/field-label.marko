import Label from "../label/label.marko";
import { cn } from "#lib/utils.ts";
import { fieldLabel as styles } from "./classes.ts";

export interface Input extends Marko.Input<"label"> {}

<const/{ class: className, content, ...rest }=input/>

<Label
  data-slot="field-label"
  class=cn(styles.root, styles.nested, className)
  ...rest
>
  <${content}/>
</Label>
