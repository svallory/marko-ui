import { cn } from "#lib/utils.ts";
import { card as styles } from "./classes.ts";

export interface Input extends Marko.Input<"div"> {
  size?: "default" | "sm";
}

<const/{ class: className, content, size = "default", ...rest }=input/>

<div
  data-slot="card"
  data-size=size
  class=cn(styles.root, className)
  ...rest
>
  <${content}/>
</div>
