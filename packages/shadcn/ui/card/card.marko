import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"div"> {
  size?: "default" | "sm";
}

<const/{ class: className, content, size = "default", ...rest }=input/>

<div
  data-slot="card"
  data-size=size
  class=cn("mu-card group/card flex flex-col", className)
  ...rest
>
  <${content}/>
</div>
