import { cn } from "#lib/utils.ts";
import { description as styles } from "./classes.ts";

export interface Input extends Marko.Input<"p"> {}

<const/{ class: className, content, ...rest }=input/>

<p data-slot="popover-description" class=cn(styles.root, className) ...rest>
  <${content}/>
</p>
