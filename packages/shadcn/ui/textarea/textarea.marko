import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"textarea"> {}

<const/{ class: className, ...rest }=input/>

<textarea
  data-slot="textarea"
  class=cn(
    "mu-textarea flex field-sizing-content min-h-16 w-full outline-none placeholder:text-muted-foreground disabled:cursor-not-allowed disabled:opacity-50",
    className,
  )
  ...rest
/>
