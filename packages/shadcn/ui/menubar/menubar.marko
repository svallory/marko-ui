import { cn } from "#lib/utils.ts";
import { menubar as styles } from "./classes.ts";

export interface Input extends Marko.Input<"div"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<div
  data-slot="menubar"
  ...rest
  class=cn(styles.root, className)
>
  <${content}/>
</div>
