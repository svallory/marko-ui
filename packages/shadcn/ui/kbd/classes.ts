export const kbd = {
  root: "mu-kbd pointer-events-none inline-flex items-center justify-center select-none",
} as const;

export const group = {
  root: "mu-kbd-group inline-flex items-center",
} as const;
