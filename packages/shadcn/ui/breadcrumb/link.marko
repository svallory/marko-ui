import { cn } from "#lib/utils.ts";
import { link as styles } from "./classes.ts";

export interface Input extends Marko.Input<"a"> {}

<const/{ class: className, content, ...rest }=input/>

<a data-slot="breadcrumb-link" class=cn(styles.root, className) ...rest>
  <${content}/>
</a>
