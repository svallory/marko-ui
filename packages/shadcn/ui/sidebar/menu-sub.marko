import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"ul"> {}

<const/{ class: className, content, ...rest }=input/>

<ul
  ...rest
  data-slot="sidebar-menu-sub"
  data-sidebar="menu-sub"
  class=cn("mu-sidebar-menu-sub flex min-w-0 flex-col", className)
>
  <${content}/>
</ul>
