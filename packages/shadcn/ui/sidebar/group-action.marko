import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"button"> {}

<const/{ class: className, content, ...rest }=input/>

<button
  ...rest
  type=input.type ?? "button"
  data-slot="sidebar-group-action"
  data-sidebar="group-action"
  class=cn(
    "mu-sidebar-group-action flex aspect-square items-center justify-center outline-hidden transition-transform group-data-[collapsible=icon]:hidden after:absolute after:-inset-2 md:after:hidden [&>svg]:shrink-0",
    className,
  )
>
  <${content}/>
</button>
