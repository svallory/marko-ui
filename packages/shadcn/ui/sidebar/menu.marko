import { cn } from "#lib/utils.ts";
import { menu as styles } from "./classes.ts";

export interface Input extends Marko.Input<"ul"> {}

<const/{ class: className, content, ...rest }=input/>

<ul ...rest data-slot="sidebar-menu" data-sidebar="menu" class=cn(styles.root, className)>
  <${content}/>
</ul>
