import SidebarProvider from "@marko-ui/shadcn/ui/sidebar/provider.marko";
import SidebarTrigger from "@marko-ui/shadcn/ui/sidebar/trigger.marko";
import Separator from "@marko-ui/shadcn/ui/separator/separator.marko";
import Breadcrumb from "@marko-ui/shadcn/ui/breadcrumb/breadcrumb.marko";
import BreadcrumbItem from "@marko-ui/shadcn/ui/breadcrumb/item.marko";
import BreadcrumbLink from "@marko-ui/shadcn/ui/breadcrumb/link.marko";
import BreadcrumbPage from "@marko-ui/shadcn/ui/breadcrumb/page.marko";
import BreadcrumbSeparator from "@marko-ui/shadcn/ui/breadcrumb/separator.marko";
import AppSidebar from "./app-sidebar.marko";

// NOTE: the source renders inside a shadcn `SidebarInset` (Sidebar
// variant="inset"), which floats the content area as a rounded, shadowed
// panel offset from the sidebar. Our Sidebar has no `variant` prop, so the
// inset look is composed directly on the content wrapper below instead.
// Deviation from the source, noted per block-porting rules.

<SidebarProvider width="16rem">
  <@sidebar|{ open }|>
    <AppSidebar open=open/>
  </@sidebar>
  <@content|{ open, toggle }|>
    <div class="relative flex min-w-0 flex-1 flex-col bg-background md:m-2 md:ml-0 md:rounded-xl md:shadow-sm md:peer-data-[state=collapsed]:ml-2">
      <header class="flex h-16 shrink-0 items-center gap-2">
        <div class="flex items-center gap-2 px-4">
          <SidebarTrigger open=open toggle=toggle class="-ml-1"/>
          <Separator orientation="vertical" class="mr-2 h-4"/>
          <Breadcrumb>
            <BreadcrumbItem class="hidden md:block">
              <BreadcrumbLink href="#">Build Your Application</BreadcrumbLink>
            </BreadcrumbItem>
            <BreadcrumbSeparator class="hidden md:block"/>
            <BreadcrumbItem>
              <BreadcrumbPage>Data Fetching</BreadcrumbPage>
            </BreadcrumbItem>
          </Breadcrumb>
        </div>
      </header>
      <div class="flex flex-1 flex-col gap-4 p-4 pt-0">
        <div class="grid auto-rows-min gap-4 md:grid-cols-3">
          <div class="aspect-video rounded-xl bg-muted/50"></div>
          <div class="aspect-video rounded-xl bg-muted/50"></div>
          <div class="aspect-video rounded-xl bg-muted/50"></div>
        </div>
        <div class="min-h-[100vh] flex-1 rounded-xl bg-muted/50 md:min-h-min"></div>
      </div>
    </div>
  </@content>
</SidebarProvider>
