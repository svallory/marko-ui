import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"ul"> {}

<const/{ class: className, content, ...rest }=input/>

<ul ...rest data-slot="sidebar-menu" data-sidebar="menu" class=cn("flex w-full min-w-0 flex-col gap-0.5", className)>
  <${content}/>
</ul>
