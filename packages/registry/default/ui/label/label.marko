import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"label"> {}

<const/{ class: className, content, ...rest }=input/>

<label
  data-slot="label"
  class=cn(
    "mu-label flex items-center select-none group-data-[disabled=true]:pointer-events-none peer-disabled:cursor-not-allowed",
    className,
  )
  ...rest
>
  <${content}/>
</label>
