import { cn } from "#lib/utils.ts";

export interface Input extends Marko.Input<"div"> {
  content?: Marko.Body;
}

<const/{ class: className, content, ...rest }=input/>

<div
  data-slot="menubar"
  ...rest
  class=cn("mu-menubar flex items-center", className)
>
  <${content}/>
</div>
