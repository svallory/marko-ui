/**
 * next/script shim — renders nothing. The example files that import this
 * use it for analytics/third-party script loading, irrelevant to a
 * screenshot-only parity harness.
 */
export default function Script() {
  return null
}
